# PiG Dream! 抓猪2.0 · 全量验收

日期：2026-08-29。仅隔离开发，未正式上线、未发群公告、未推送远端。

结论：**离线全量验收通过**。最终1,819项自动测试、676个不同素材/功能图片场景通过；
生产快照迁移、可靠历史补发及回滚恢复通过。真实QQ收发和正式上线未执行。

## 1. 基线与结论边界

- 验收起点：`develop/2.0`，`0c997ff`，`2.0.0.dev11 / Schema45 / Ruleset39 / Manifest4`。
- 本次修复版本：`2.0.0.dev12 / Schema46 / Ruleset40 / Manifest4`。
- 运行环境：Windows、Python3.13.12、SDK2.7.1；依赖锁未升级。
- 开发目录位于MaiBot插件发现目录之外，使用独立数据库、素材副本、缓存和无头浏览器。
- 正式插件基线仍为`ae1d221 / 1.29.1 / Schema34 / Ruleset29`，未切换代码、迁移原库或重启进程。

本报告覆盖自动化、规则/代码审查、正式素材实渲、合成容量、生产快照双迁移、补发、故障恢复及
旧代码回滚。它不代表已经在真实QQ群进行2.0玩法，也不是用户的维护/发布批准。
后续必须完成第8节门禁，不能以“全量测试通过”代替真实通道和维护确认。

## 2. 本轮实际修复

### 三格徽章从资源补成完整机制

- 默认1格；500成就点里程碑已授予的`badge-showcase-3`永久权益开放3格。
  不允许只改展示点数、猜徽章ID或跨群查询绕过归属校验。
- `/成就徽章`查询；`/成就徽章 查看 2`翻页；`/成就徽章 2 徽章名或ID`佩戴；
  `/成就徽章 卸下 2`卸下。已拥有的普通徽章、宝箱徽章和周榜牌都可使用。
- Schema46保存独立槽位，并对归属、位置和重复佩戴建立约束；旧合法单槽迁入第1格。
- 抓猪、做菜、档案、背包、成就、排行、派遣、巡演和对战共享真实槽位投影。
- 纯外观回执不触发成就历史补发、周榜计分或经济奖励；重放/重启不会恢复过时设置。
- 最后全量回归还发现旧`badge_name`兼容字段错误返回内部ID，现恢复中文名称，
  新多槽规范ID仍保持稳定。新增6条兼容回归，未靠放宽原名称断言通过。

详见[三槽交付记录](design/phase4/badge-showcase-acceptance.md)。

### 巨物成就与历史查询

- 原成就事件把“体长或体重达标”同时计入长度、重量和双项成就。现按已提交的巨物目击事实
  分轴计数，用实例、作用域和原抓取玩家定位，不按后改的模板阈值重判历史。
- 八项确定性回归覆盖不达标、单轴、双轴、阈值变更、他人/他群/其他实例及重放；
  没有追溯扣除开发库中以前授予的奖励，更没有改动生产玩家。
- 历史寿司取得查询改为先合并该玩家的持有、做菜生成和转入实例ID，再按主键读取菜品；
  去掉逐菜品重复扫描回执的关联子查询。消费、售出、转出、跨来源去重和管理员来源过滤不变。
- 实例最多样本的旧查询3秒内未完成，新查询约31ms；全350人回填首次查询中位41.87ms、
  最大335.88ms。未据此做无证据的全库重构或修改奖励数值。

### 图卡与验收工具

- 结果卡右侧固定宽度加边距超过含边框父容器内容宽度2px；增加基于父内容宽度的上限。
  不移动`38,164,480,480`动画合成槽，不用隐藏溢出裁字；13张旧页与长效果、新卡、徽章图复验通过。
- 动画验收原先在WebP帧解码前读取时长，可能误报为0；现先解码再读，新增“真一致通过、真差异拒绝”测试。
- 新菜验收样本改用当前正式定义的真实效果，不再展示旧“待设计”占位说明。
- 备用立绘逐项验收发现初华猪卡片误写保千指令，现按对应猪名生成真实切换命令并带实例编号；
  初华明确“普通版/戴帽子版”，保千保留“立绘/表情包”。不可见媒体及未知备用形态不捏造指令。
  三个平台的真实处理器→HTML断言和7个适配器边界用例一并补齐。
- UAT组件核查不再要求早期55命令，改验唯一名称、可调用处理器、有效正则、关键功能入口和单一HOME_CARD。
  完整注册表回归仍明确断言84个Command与1个HOME_CARD，不丢掉新增路由约束。
- 克隆工具强制只读源、新建输出、目录不重叠、解析路径不越界，防止验收时写回原库或覆盖唯一备份。
  专注单条业务出图的UAT关闭成就统计，防止额外成就通知被误判为重复发送；成就开启场景另有完整测试。

## 3. 自动化与需求覆盖

最终全量pytest：**1,819通过、0失败、0跳过，210.54秒**，比dev11基线新增75个用例。
Ruff、compileall、元数据/JSON/TOML契约、离线锁文件、Git差异与Git LFS对象/指针检查均通过。
最终JUnit结果保存在忽略目录，报告数字不累加此前重跑次数。
初始dev11基线为1743通过、1个过时Schema断言失败；修复过程中曾出现2个兼容失败，
分别是新命令数量和徽章名称，均逐项修复后重新跑整仓，不把失败轮次算成通过。

| 验收域 | 主要覆盖 |
| --- | --- |
| 抓猪/做菜/菜品/道具 | 六档概率、永久与临时分层、六星独占、队列余量、滚动/时段额度、轮盘、新菜奖池与满层奖励 |
| 专属机制 | 专属原料池、领域接管与实际转菜、苍赫归属、茈五只实际资产、全家桶人数/实付总额、事件剩余次数 |
| 资产与社交 | 收藏、联动最高价值留一、同名选择、最后一菜确认、编号释放、赠送/交易、并发报价、名单及管理权限 |
| 成就/周榜 | 130项条件与旧49毕业集合、自然来源、隐藏遮罩、历史补发、奖励券/宝箱/外观、名次与结算幂等 |
| 派遣/巡演 | 低星材料、占用与返程、十团50身份/52形态、混团、培养、三站/一键等价、联演与奖励券 |
| 对战 | 等权招式、累计胜利权重、贷款/零招、无限核心、伤势、五级成长、挑战时限、终局与败者五次额外交付 |
| 帮助与接入 | 16专题、真实配置/开关、原路由兼容、前导@、`qq`与两个官方接入身份的离线模拟；不泄漏彩蛋 |
| 数据安全 | 逐级迁移、结构守卫、事务回滚、重放/重启、跨玩家/四群隔离、账本与备份恢复 |
| 渲染和资源 | 图片优先、文字兜底、隐藏/缺图、长文/分页、原动画、缓存限额、发送队列及超时幂等 |

当前84个命令的主出口为71条玩家图片路由、11条管理文字路由、2条可复制帮助。
错误、取消、权限拒绝、确认提示和图片故障仍允许文字，不能把“图优先”说成所有分支必出图。

本轮未再次调整商城价格或已确认概率。对战每组3000场离线抽样：同等级宿傩约47.1%–47.2%、
五条约52.8%–52.9%，高5级宿傩对0级五条约54.2%，0级宿傩对5级五条约40.1%；
平均约4.1轮，样本最长18轮。这是抽样观察，不是胜率保证，也未给核心叠层新增上限。

## 4. 正式素材与图片

最终使用Chromium实际生成并检查 **676个不同验收场景**；另有82次三槽接线后和107次CSS修复后
复渲，不重复计入676。正式素材预览、视图模型布局样本和真实服务回执分别由各套报告标明。

| 场景组 | 数量 |
| --- | ---: |
| 全部正式猪/菜素材卡 | 328 |
| 备用立绘（四群保千及初华戴帽版） | 5 |
| 派遣 / 巡演 / 对战 | 20 / 28 / 25 |
| 成就 / 首期周榜 | 55 / 4 |
| 菜品奖励 / 群事件与新功能回执 / 道具背包 | 18 / 16 / 13 |
| 收藏/占用等状态 / 第九期新结果卡 | 11 / 70 |
| 做菜经济 / 社交排行 / 标签吨级边界 | 16 / 9 / 13 |
| 四个作用域完整猪菜图鉴 | 8 |
| 抓猪收藏旧页 / 长效果与外观结果 / 真实三槽 | 13 / 12 / 12 |

- 当前正式目录223猪、105菜，共328模板；257份唯一二进制，主/备用图共333次引用、332个不同路径，
  含20个动画模板、96个群专属模板。素材目录另有一份未引用旧文件，保留未删，不影响运行。
- 每个授权群可见187猪、69菜（12猪/12菜六星），四群图鉴在同样虚构玩家状态下输出一致；
  这验证内容同步，不代表不同群玩家数据共用。
- 检查文字裁切、元素越界、坏图、媒体裁切；最终诊断为0。原始素材哈希未改，动画帧数、时长和循环守恒。
- 查看28张正式素材联系表、全部20个动画条带及代表性完整图和320px缩略；
  名称、主要数值和概率在缩略可辨，长篇效果需要点开原图，不宣称长图全文缩小后仍清晰。
- 四群完整猪图鉴1128×10557、约3.19MB，菜图鉴1128×4471、约1.09MB；
  使用有界预览，未把全部原始媒体直接嵌入页面。
- 947揭晓是基于用户给定原画的插件揭晓动画，不冒充原游戏完整抽卡动画；
  四种揭晓实测6帧、总时长5180ms，奖品由已提交回执决定，不为出图再次随机。
- 两张完整结果GIF约11.18MB和8.88MB，低于插件当前50MiB动画上限；
  **真实QQ上传能力未验收**，不擅自通过删帧、替换原图或静态图假称动画送达。

## 5. 生产快照迁移、回填及经济影响

生产连接只用SQLite `mode=ro`、`query_only=ON`及在线备份接口；未复制运行中的WAL/SHM，
所有迁移与发奖都在`artifacts/final-acceptance-20260829/`中的独立副本完成。

输入快照：Schema34、350玩家、4作用域、86,149猪实例、31,866菜实例、134,973业务回执、
149,277币账本行、64交易、3,460转移事实，余额合计20,994,517币；数据库720,027,648字节。
完整性为ok、外键0异常、余额对账0差异。

两份新建副本分别约6.34秒、7.28秒完成34→46升级。逐表逐旧字段比对，并验证重开数据摘要不变：

- 原猪/菜编号、实例身份、价值、体型、重量、余额、回执、交易、名单、审计和其他历史不被无故改写。
- 允许已确认的v44/v45效果迁移，本快照影响13个菜模板与25份符合条件的存量菜；
  不能把“保留旧资产”误写成“所有字段绝不迁移”。未使用队列只按冻结迁移规则处理。
- 再导入当前328项正式素材，旧实例和历史回执仍保留当时快照。
- 当前1.x实际旧代码拒绝Schema46，不会误当旧库运行；从升级前备份另建Schema34恢复库，
  旧代码打开后完整数据摘要不变。

对全部350名历史玩家各执行一次回填，再完整执行第二遍：

| 项目 | 副本结果 |
| --- | ---: |
| 新解锁 | 4,659 |
| 猪币奖励 | 4,587,000 |
| 玩法券总张数 | 746 |
| 徽章 / 称号 / 边框 | 620 / 20 / 223 |
| 宝箱 / 展示架权益 | 27 / 49 |
| 尚未完成回填玩家 | 0 |
| 重复执行增加的奖励、解锁或账本 | 0 |

这批猪币约为快照原总余额的21.8%，是已设计的完整历史奖励，并非本轮额外加奖。
正式上线前应重新评估最新快照；未经另行确认，不调低既定奖励、不先向真实群发奖、
不把测试副本覆盖原库。正式新派遣/巡演/对战成就只依据上线后真实事实，测试事实不会带回生产。

验收工具的报告只输出聚合数量与摘要，原数据库及恢复副本留在Git忽略目录，不提交仓库。

## 6. 恢复、端到端与容量

### 故障恢复12步

正常抓猪、一次图片发送失败并完整文字降级、同进程重放、缺图占位、SQLite写锁安全失败、
报价、重启重放、过期解锁、第二群抓猪、双群排行及恢复库重新加载全部符合预期。
共7次成功图片交付、2次预期文字交付（故障降级/忙碌提示）；故障日志来自本地注入，不是生产报错。

备份恢复前后玩家353、猪86,152、菜31,866、回执134,977、交易65、账本153,858完全一致；
重复消息不增加资产/账本，忙碌写事务不留下半成品，账本0差异、quick_check=ok。
恢复测试中使用虚构新玩家，未向真实群发消息。

### 新机制命令到图片13步

食用轮盘→抽轮盘、领域食用→发动→抓猪自动转菜、苍/赫食用→发动→实际归属结算、
茈五猪结算、全家桶实收人数/合计：13条命令均输出1张核心业务图片，0次文字兜底。
测试副本把群术式余次缩为1以覆盖一次结束路径；完整10/5次与并发语义另由自动回归验证。
此脚本关闭成就通知，仅验证核心图片；开启成就时的通知、幂等与外观纯操作另有测试。

### 合成容量

- 1,000模拟玩家、10,000事实全部完成结算，分76批；单批中位293.475ms、最大507.45ms。
- 空队列中位2.89ms；单玩家投影最大414字符。
- Python追踪到的增量峰值381,661字节，数据库和独立已验证备份各17,420,288字节。
  这不是整机或整个进程内存，也不是QQ延迟或生产高峰SLA。
- 完整性、外键、材料账本、备份恢复和重复发奖检查全部通过。

## 7. 证据与复现

本地证据根：`artifacts/final-acceptance-20260829/`，不纳入Git：

- `pytest-final.xml`：最终整仓测试。
- `visuals/visual-summary.json`：676场景汇总；每套子目录有图片、DOM诊断与动画校验。
- `production-clone-final/report.json`：有效的双迁移、旧字段核对、回填与回滚报告。
- `production-clone-final/pre-upgrade.sqlite3`及`verified-v2-backup.sqlite3`：只读来源保护点与已验证副本备份。
- `production-recovery/report.json`、`command-mechanics-final/report.json`、`capacity-final/report.json`。
- `rules/rules-audit.md`：规则问题与只读慢查询对照。初次失败/中断目录不作为最终验收结果。

完整命令及参数见[工具手册](../tools/README.md)。示例均从隔离开发目录执行，输出必须使用新目录：

```powershell
.venv/Scripts/python.exe -X utf8 -m pytest --junitxml=artifacts/acceptance-rerun/pytest.xml --disable-warnings --tb=short
.venv/Scripts/python.exe -m ruff check pig_catcher plugin.py tools tests
.venv/Scripts/python.exe -m compileall -q pig_catcher plugin.py tools tests
uv lock --check --offline
git diff --check
.venv/Scripts/python.exe tools/accept_v2_production_clone.py --source-database <维护前只读快照> --legacy-code <1.x代码目录> --output artifacts/acceptance-rerun/production
.venv/Scripts/python.exe tools/uat_production_recovery.py --data-dir artifacts/acceptance-rerun/production/migration-a --output artifacts/acceptance-rerun/recovery --browser-executable <Chrome路径>
.venv/Scripts/python.exe tools/accept_badge_showcase.py --help
```

占位参数需换成真实路径，不带尖括号。迁移工具只能写新建的隔离输出；
禁止对生产目录运行开发版迁移、禁止用旧代码直接加载已升级数据库。

## 8. 尚未执行，不得冒充通过

1. 真实QQ双Bot：官方图片/GIF上传、真实提及/昵称、客户端缩略、两人在场对战和网络重投。
   当前只完成平台身份与消息路由的离线模拟，没有发送真实消息。
2. 用户确认正式数值、完整历史奖励补发、首期活动的实际起止时间与内容冻结；
   成就/周榜开关和图片最短边180px等生产自定义配置不能用仓库默认值静默覆盖。
3. 冻结RC/发布包、核对Git LFS素材完整性、确定维护/公告/回滚方案，再取得最新只读快照复验。
4. 用户批准后才发布、切换MaiBot或发送群公告。本次没有执行这些动作。

交接结论应表述为“隔离开发与离线全量验收通过，真实通道与正式上线尚待批准”，
不能写成“两个官方群已经升级完成”或“以后不会再出现任何bug”。
