# 开发路线、验收与待确认项

## 1. 分轮计划

### 第一轮：需求与工程准备

状态：已完成设计产物，待本轮最终审计。

- 完整需求解析
- 领域、数据、命令、概率、经济和视觉设计
- 第二轮素材交付规范
- 独立 Git 仓库
- Codex 专用 Skill
- 不创建插件运行框架

### 第二轮：完整框架与素材管线

输入：用户提供的第一批正式素材和对待确认项的结论。

第二轮拆成两个可独立验收的小阶段。

#### 2A：无正式素材框架期

状态：已完成并通过本地工程验收。

- 标准 `_manifest.json`、`plugin.py`、配置模型和包结构
- SQLite 初始化、迁移器、仓储接口和事务管理
- 命令注册骨架，但不使用普通消息监听或 LLM
- 素材清单协议、合成测试素材校验和 6 星群素材隔离
- 白底淡粉渲染主题、字体、占位图和基础模板
- 随机源、时钟、消息 ID 和发送器的可测试接口
- pytest、编译、Manifest、加载和卸载验证

占位图只用于缺图和隐私保护，不进入可抽取模板池。未闭环的玩法命令不得返回假成功。

#### 2B：正式素材接入期

状态：已完成并通过本地工程、全素材视觉和运行中宿主加载验收。

已完成：

- 1 至 5 星公共素材清单、授权、属性范围和真实加载
- 6 星素材的所属群、授权、撤回与隔离实测
- 83 只猪、15 道美食及其静态整活描述
- 9 项 GIF/动画 WebP 的原动画保真合成
- 5 只 BanG Dream 联动猪与按乐队固定 `X/5` 的收集元数据
- 98 个模板在正式素材卡片和群聊缩略尺寸中的视觉验收
- 生产素材校验值、公共 Git 收录范围和真实素材 UAT

第二轮验收不以“文件存在”为准，而以空素材、合法素材、非法素材、空数据库、重启和配置更新都能通过测试为准。独立字体、图标和品牌元素未随首批素材交付，不阻塞素材目录本身的 2B 验收。

### 第三轮：抓猪与收藏

状态：已完成代码、自动化、Chromium、运行中宿主加载和正式素材隔离命令 UAT。

- 抓猪概率、属性、价值、奖励和经验
- 每日次数、冷却、道具装备
- 猪背包、详情、图鉴和群纪录
- 单次抓取、背包和图鉴图片
- 幂等与重启保护

### 第四轮：做菜与经济

状态：已完成代码、自动化、Chromium、运行中宿主加载和正式素材隔离命令 UAT。

- 做菜矩阵、肥瘦食谱、厨具和 4 种做菜道具
- 美食背包、图鉴、详情、吃菜效果接口
- 商城、升级、官方售卖和账本
- 做菜、商城和回执图片

### 第五轮：社交与排行

状态：已完成代码、自动化、Chromium 视觉验收、正式数据隔离命令 UAT 和运行中生产库迁移。

- 赠送
- 两阶段交易、过期解锁和并发保护
- 展示物设置
- 七类排行榜和排行榜图片
- 中文管理面板的完整访问控制和维护入口
- `NEW`、特殊体型与个体评价
- 全群绝对体型/重量纪录和巨物目击历史

### 第六轮：全量实机验收

状态：自动化故障恢复、正式素材副本、生产备份恢复和运行中宿主验收已完成；真实 QQ
群内的人机命令回归按用户明确决定在后续实际使用中完成。

- MaiBot 真实加载、重载和停止
- QQ 群内完整命令回归
- 多用户、多群、重复消息和并发交易
- 图片发送失败、数据库繁忙、素材缺失和重启恢复
- 账本对账、备份和恢复演练

## 2. 全量验收标准

### 2.1 功能

- 六档抓猪概率和所有修正与设计一致。
- 每只猪具有有效且相关的体型、重量、肥瘦率和价值。
- 最大体型与最大重量分别正确更新群纪录。
- 迷你与巨型模板使用显著不同的真实属性范围，达到门槛的巨物进入当前群绝对纪录与目击历史。
- 1 至 5 星公共素材可用，6 星素材严格按群隔离。
- 背包、图鉴、做菜、吃菜、商城、升级、售卖、赠送、交易、展示和排行均完整。
- 6 星猪做菜只产生 5 星或 6 星，频率为 90/10。
- 六阶段等级和经验累计正确。

### 2.2 交互与视觉

- `/抓猪帮助` 是便于复制的纯文字。
- 所有业务查询和成功结果都有白色、淡粉主视觉的独立渲染图。
- 不复刻参考图主题和布局。
- 长名称、缺图、零数据、满页和多页都不重叠、不溢出。
- 图片发送失败时有完整文字兜底，业务不回滚。

### 2.3 数据与安全

- 不监听普通消息，不调用 LLM，不修改 MaiBot 核心。
- 跨群查询、赠送、交易和 6 星素材读取全部被拒绝。
- 重复消息不会重复发奖、扣物、转账或公示。
- 进程重启后幂等保护、报价锁和资产状态仍有效。
- 余额不为负，账本可重算，交易双方和物品在一个事务中完成。
- 未授权或已撤回的群友素材不再用于新抽取。

### 2.4 工程质量

- `pytest` 全通过。
- Python 编译和 Ruff 检查通过。
- Manifest 与配置解析通过。
- 插件加载、卸载、配置更新和重载通过。
- 渲染截图完成桌面尺寸和群聊缩略图检查。
- 数据库从空库初始化、逐级迁移和备份恢复通过。

## 3. 必测场景

1. 首次抓取、重复模板抓取、打破体型纪录、打破重量纪录和两项同时破纪录。
2. 群 A 有 6 星素材、群 B 没有；群 B 永远不能抽到或查看群 A 素材。
3. 同名猪有三只，未给短编号时不得误消耗。
4. 1 星猪与 5 星猪的做菜概率边界。
5. 6 星猪在固定随机源下覆盖 5 星和 6 星结果，永不出现其他结果。
6. 做菜数据库提交成功但图片发送失败，原料只消耗一次且美食仍存在。
7. 同一抓猪、售卖、赠送、接受交易消息重复投递。
8. 两个接收请求同时接受同一报价，最多一个成功。
9. 报价后物品不能售卖或再次报价，过期后自动解锁。
10. 余额恰好足够、不足和并发扣款。
11. 素材路径逃逸、损坏图片、重复模板 ID 和授权撤回。
12. 长昵称、长物品名、空背包、满背包和按品质完整图鉴长图渲染。
13. 特小猪与大象的模板范围、`NEW`、体型评价和双项巨物公示。
14. 赠送与成交后累计抓取统计不倒退，成交账本两条分录合计为零。
15. 综合、抓猪、美食、价值、巨物、数量和猪币七类排行均只包含当前群玩家。

## 4. 第二轮素材交付清单

用户提供素材时，最好同时给出：

- 1 至 5 星猪素材目录
- 每张素材对应的猪名、品质和一句描述
- 体型与重量的大致合理区间；没有时允许第二轮提出建议值
- 偏瘦、均衡或偏肥标签
- 首批美食素材及名称；若暂缺，可先只建立食谱元数据接口
- 哪些素材属于 6 星、对应哪个群、是否获得使用授权
- 联动素材的名称、授权、启用时间和是否限时
- 允许分发的字体、图标或品牌元素

原始素材无需按参考图排版，也不要提前把名称、星级和数字写死在图片上。

## 5. 待确认决策

以下项目已给出默认建议，第二轮开始前由用户确认或调整：

| 项目 | 默认建议 |
| --- | --- |
| 正式插件名称 | 暂用“抓猪插件” |
| 目录与插件 ID | `pig_catcher` / `local.pig-catcher` |
| 数据范围 | 每个平台群独立资产、币、等级、纪录和排行 |
| 做菜是否消耗猪 | 是，成功事务中永久消耗 |
| 赠送是否需要接收 | 不需要，发出后立即原子转移 |
| 交易方式 | 两阶段报价，接收方显式接受 |
| 抓猪频率 | 每群每人于北京时间 00:00、09:00、12:00、19:00 各刷新 5 次，成功抓取后间隔 20 秒 |
| 交易有效期 | 5 分钟 |
| 6 星素材 | 群管理员启用、当事人授权、支持撤回，不自动抓头像 |
| 6 星素材进 Git | 默认不进入公共 Git 历史 |
| 具体美食效果 | 等后续素材与玩法补充 |
| 正式作者、许可证和远程仓库 | 2A 暂用 `buruqu`、MIT 和 `https://github.com/buruqu/pig-catcher`；远程尚未创建或推送，发布前可调整 |
| 接受交易时余额不足 | 保持报价待处理直到拒绝、取消或过期 |
| 特小猪属性 | `4–16 cm / 0.35–6 kg`，`mini` 体型档案 |
| 大象属性 | `120–260 cm / 350–1800 kg`，`giant` 体型档案 |
| 巨物默认门槛 | 体型 `120 cm` 或重量 `350 kg`，按群留档 |

若用户未另行说明，后续轮次继续按这些默认建议实现。

## 6. 变更控制

- 产品规则变化先更新文档和规则版本，再改代码与测试。
- 概率、价值和奖励变化附带数值模拟，不直接凭感觉上线。
- 素材变化通过清单和校验值管理，不覆盖历史来源信息。
- 每轮结束必须更新验收状态、已知缺口和 Git 提交。
- 未经用户明确要求，不推送远程仓库。

## 7. 2A 验收记录

验收日期：`2026-07-28`

- MaiBot `1.0.12`、SDK `2.7.0`、Python `3.14.4`
- pytest：`72 passed`
- Ruff：通过
- Python `compileall`：通过
- Manifest v2 严格校验：通过，无错误、无警告
- MaiBot `PluginLoader`：成功加载 `PigCatcherPlugin`
- 组件边界：仅 `COMMAND pig_catcher_help`，无普通消息监听、Tool、LLM 或网关
- 生命周期：加载、配置关闭、配置重开和卸载通过
- SQLite：空库迁移、回滚、完整性检查、在线备份和未来版本拒绝通过
- 幂等：同消息重复预留、一次发送领取及重启后不重发通过
- 素材：合法/非法清单、损坏图片、路径逃逸、重复 ID、群隔离和撤权通过
- 渲染：真实 Chromium 生成 `1128x1079` PNG，480px 群聊缩略图无重叠或溢出
- 命令级手工 UAT：正式 `PluginLoader` 加载后，`/抓猪帮助` 与 `/抓猪帮助 做菜` 均成功返回纯文字，渲染和发图调用均为 0
- 热更新手工 UAT：用户黑名单即时生效，插件停用后数据库关闭，重新启用后数据库重开，最终卸载无残留运行引用
- 当前未做运行中 MaiBot/真实 QQ 玩法 UAT；2A 只有纯文字帮助，不把模拟宿主测试冒充实机验收

## 8. 2B 验收记录

验收日期：`2026-07-28`

- 正式目录：`98` 项，包含 `83` 只猪、`15` 道美食、`9` 项动画和 `4` 项群专属素材
- 品质：猪 `20/20/19/13/9/2`；美食 `1/4/5/2/1/2`
- 全量读取：98 项均完成格式探测和逐帧解码，无损坏文件
- 动画：8 个 GIF 内容和 1 个动画 WebP 内容均完成逐帧卡片合成；原始校验值不变
- 动画兼容：覆盖 96 帧翻滚、36 帧旋转、24 帧群专属动画及无显式时长素材
- 联动图鉴：Poppin'Party、Afterglow、MyGO!!!!! 的 5 个已交付角色槽位均持久化，系列进度固定显示 `X/5`
- 群隔离：`qq:1092931381` 可见 `83/15`，另一测试群仅可见 `81/13`
- SQLite：Schema `2`、`quick_check=ok`、98 个素材文件与数据库校验值完全一致
- Chromium：98 张白色淡粉卡片、9 张联系表和 9 组代表帧条带均无重叠、截断或错误静止
- 独立环境：`uv lock --check`、`uv sync --all-groups --locked`、pytest `83 passed`、Ruff 和 `compileall` 全部通过
- 可重复渲染：独立环境第二次生成的 98 份卡片与首次人工验收版本逐文件 SHA-256 完全一致
- 运行中宿主：MaiBot Runner 成功加载 `local.pig-catcher v0.2.0`，日志确认阶段 `2B`
- 当前仍只有 `/抓猪帮助`；真实抓取、背包和图鉴命令按第三轮实现，因此没有把素材预览当作已开放玩法

## 9. 第三轮验收记录

验收日期：`2026-07-28`

- 插件：`local.pig-catcher v0.3.0`，阶段 `3`，Schema `3`
- 组件边界：9 个显式 `COMMAND`；`/抓猪` 与 `/抓群友` 共用一个组件；无普通消息监听、Tool 或 LLM
- 命令：帮助、抓取、档案、详情、背包、图鉴、群纪录、使用道具和取消道具均完成
- 抓取事务：实例、猪币、经验、图鉴、体型/重量纪录、道具消耗和回执原子提交
- 幂等：同进程重复、两个数据库管理器并发重复和插件重启后重复均只提交一次且不重复发图
- 频率：北京时间自然日次数、跨午夜冷却和每日上限均有自动化覆盖
- 群边界：授权群最高随机边界抽到六星；无六星普通群同一边界只到五星
- 道具：装备和取消不消耗；成功抓取恰好消耗一个；冷却失败不消耗
- 渲染：13 个代表视图经 Chromium 检查，长文、空页、满页、480px 缩略图均无溢出、遮挡和坏图
- 动画：正式六星抓取输出 24 帧循环 GIF；详情同样保持动态，列表不泄露静态首帧
- 正式素材隔离 UAT：13 次命令调用，11 次预期图片发送、2 次重复消息零发送；3 个抓取对象对应 3 个唯一回执
- 自动化：pytest `99 passed`，Ruff 和 Python `compileall` 通过
- 运行中宿主：MaiBot Runner 日志确认 `local.pig-catcher v0.3.0` 加载、注册和阶段 `3`，无插件级警告或错误

已知缺口：

- 赠送、两阶段交易、展示与排行仍属第五轮。
- 正式美食模板暂未配置特殊 `effect_id`；当前食用效果为按品质获得经验，特殊效果与六星超级效果待规则输入后扩展。
- 本轮命令 UAT 使用生产数据库和素材的一致性隔离副本，并使用真实 Chromium；尚未把测试消息发送到真实 QQ 群，真实 QQ 多群故障注入保留到第六轮。

## 10. 第四轮验收记录

验收日期：`2026-07-28`

- 插件：`local.pig-catcher v0.4.0`，阶段 `4`，Schema `4`，Ruleset `2`
- 组件边界：19 个显式 `COMMAND`；包含 `/抓群友` 与 `/使用美食` 两个命令别名；无普通消息监听、Tool 或 LLM
- 频率：默认每群每人北京时间自然日成功抓取 20 次，成功抓取后冷却 20 秒；默认配置与实际服务行为均有测试。连续流程 UAT 仅在隔离副本中临时使用 `100 次 / 0 秒`
- 默认频率手工验证：第一次 `/抓猪` 返回“今日抓猪：1/20”并发送 1 张图；紧接着第二次 `/抓猪` 返回“抓猪冷却中，还需等待 20 秒”
- 做菜边界：1 至 5 星猪永不产生六星菜；六星猪只产生五星或六星菜，严格保持 90/10 且不受道具与升级修改
- 做菜事务：原料猪、食物实例、图鉴、猪币、经验、道具、额外餐盒产物和命令回执原子提交；缺失食谱或状态冲突整体回滚
- 美食：详情、背包、图鉴、食用与 `/使用美食` 别名完成；未知特殊效果会在消耗前拒绝，空效果安全获得品质经验
- 商城：8 种消耗品、饲料与厨具五级升级、数量购买、余额不足和满级拒绝完成
- 售卖与账本：猪和美食按快照官方价值售卖；所有猪币变化进入不可变账本，缓存余额逐笔对账通过
- 幂等：做菜、购买、食用与售卖同进程重复、并发及插件重启后重复均不重复资产、扣款或发图
- 群边界：授权群六星猪可制作群专属六星菜；普通群不可读取、选择或产出其他群六星资产
- 渲染：11 个代表视图、长名称、空页、满页、隐私图鉴与 480px 缩略图经真实 Chromium 检查，零溢出、零遮挡、零坏图
- 可解释性：做菜图与文字兜底均展示最终正权重档位；六星实测显示 `5★ 90.0% · 6★ 10.0%`
- 正式素材隔离 UAT：20 次命令调用，17 次预期图片发送、3 次重复消息零发送；插件重启后重复做菜零发送
- UAT 数据：隔离库最终 `pig_instances=4`、`food_instances=3`、`currency_ledger=11`、`command_receipts=11`
- 自动化：pytest `111 passed`，Ruff 和 Python `compileall` 通过
- 运行中宿主：MaiBot Runner 日志确认 `local.pig-catcher v0.4.0` 加载、注册和阶段 `4`；Runner 汇总 `loaded=20 failed=0 inactive=3`

已知缺口：

- 赠送、两阶段交易、展示设置与七类排行榜属于第五轮，帮助中已明确标为尚未开放。
- 正式美食模板目前没有特殊 `effect_id`，所以 `/吃菜` 只提供稳定的品质经验；没有虚构素材未定义的六星超级效果。
- 本轮使用生产数据库和正式素材的一致性隔离副本、真实插件命令与真实 Chromium，但没有向真实 QQ 群发送测试消息；QQ 多群、断连和发图故障注入保留到第六轮。

## 11. 第五轮验收记录

验收日期：`2026-07-28`

- 插件：`local.pig-catcher v0.5.0`，阶段 `5`，Schema `5`，Ruleset `3`
- 组件边界：27 个显式 `COMMAND`；无普通消息监听、Tool、LLM、网关或 MaiBot 核心修改
- 抓猪频率：生产默认仍为每群每人每日 20 次、成功后冷却 20 秒；连续隔离 UAT 临时使用 `100 次 / 0 秒`
- 赠送：猪与美食必须使用精确资产选择器和恰好一个结构化 `@`，同群立即原子转移，不产生猪币或经验
- 交易：五分钟两阶段报价，发起、接受、拒绝、取消、过期和个人列表闭环；接受时重新校验角色、范围、锁、状态和余额
- 原子与并发：成交资产和双方余额在同一事务中提交；并发接受测试恰好一个成功；成交账本两条分录合计为零
- 余额不足：报价保持待处理，不扣币、不转移资产；过期任务将报价置为 `EXPIRED` 并解锁资产
- 展示与排行：猪/美食展示位完成；综合、抓猪、美食、价值、巨物、数量和猪币七类榜均完成并按群隔离
- 稳定统计：累计抓猪、做菜、赠送和成交不随资产转移、售卖或消耗倒退
- 特殊体型：特小猪 `4–16 cm / 0.35–6 kg`；大象 `120–260 cm / 350–1800 kg`
- 巨物：默认体型 `120 cm` 或重量 `350 kg` 达线，写入全群绝对纪录与巨物目击；普通模板的极端个体也会获得确定性评价
- 渲染：8 个第五轮代表视图经真实 Chromium 检查；`NEW`、迷你、双项巨物、全群纪录、赠送/交易票据、交易列表、展示档案和综合榜均为零裁切、零越界、零坏图
- 命令 UAT：24 个步骤，19 次图片发送、1 次预期余额不足文字回复；结构化赠送、成交、过期、展示、七类榜和双群隔离全部通过
- 重启幂等：赠送与接受交易在插件重启前后重复投递均为零新增公示
- 数据：生产库已迁移 Schema 5，`PRAGMA quick_check=ok`；隔离 UAT 中赠送事件 `1`、成交事件 `1`、两个群玩家数分别为 `2` 与 `1`
- 自动化：pytest `119 passed`，Ruff、Python `compileall` 和 `git diff --check` 通过

已知缺口：

- 本轮完成本机 MaiBot 生产库迁移和正式素材隔离命令 UAT，但没有向真实 QQ 群发送赠送或交易测试消息；真实适配器断连、QQ 图片发送故障和多进程崩溃窗口保留到第六轮。
- 正式美食模板仍未定义特殊 `effect_id`；这与第五轮社交功能无关。

## 12. 第六轮与正式版验收记录

验收日期：`2026-07-28`

- 插件：`local.pig-catcher v1.0.0`，阶段 `6`，Schema `5`、Asset Manifest `2`、Ruleset `3`
- 兼容性：MaiBot `1.0.12`、SDK `2.7.1`；仍只注册 27 个显式 `COMMAND`，没有普通消息监听、Tool、LLM、网关或核心代码修改
- 规则回归：生产默认仍为每群每人每天 20 次成功抓猪、成功后冷却 20 秒；概率、价格、库存和数据结构均未改动
- 图片故障：注入 QQ 图片发送异常后恰好发送一条完整文字，业务实例与回执保留；同进程和插件重启后重复消息均零新增发送
- 素材故障：删除一个正式素材文件后，单体详情仍生成白粉占位图片并标记“文件暂缺”；维护巡检报告 `1` 个缺失，恢复文件后回到 `0`
- 数据库繁忙：外部 `BEGIN IMMEDIATE` 写锁超过 100 ms 测试等待后命令稳定失败，新增实例、收据、余额和流水均为 `0`
- 重启恢复：待处理报价和资产锁跨插件重启保持一致；维护任务将过期报价置为 `expired` 并原子解锁资产
- 双群隔离：同一测试用户在两个群建立独立玩家与库存，两个综合排行榜分别只读取当前群
- 账本与恢复：隔离流程结束全库余额异常数为 `0`；在线备份恢复到新目录后表计数逐项一致，Schema `5` 且 `PRAGMA quick_check=ok`
- 第五轮完整回归：24 步通过，19 张图片、1 条预期余额不足文字；赠送、成交、过期、展示、七类排行、双群隔离、零和流水和重启幂等均保持通过
- 第六轮故障 UAT：12 步通过，7 张成功图片、2 条预期文字、1 次注入图片失败；恢复库重新加载和档案查询成功
- 自动化：pytest `124 passed`；Ruff、Python `compileall`、Manifest/config 解析、依赖锁与补丁检查通过
- 生产保护：升级前生成一致性备份 `pig_catcher-pre-v1.0.0-20260728-185711.sqlite3`，Schema `5`、`quick_check=ok`

人工项：

- 用户明确表示不需要 Codex 操作 QQ；真实群内命令由用户正式使用时自行验证。因此本轮不把本地适配器或模拟消息结果写成“真实 QQ 已验收”。
- 正式美食素材仍未定义特殊 `effect_id`，继续按品质提供稳定经验；该扩展需要未来明确的新规则与素材，不阻塞 `1.0.0` 投入使用。

## 13. 完整图鉴与白名单素材复验

验收日期：`2026-07-28`

- 插件升级为 `local.pig-catcher v1.1.0`；Schema `5`、Asset Manifest `2`、Ruleset `3` 均未改变。
- `/猪猪图鉴` 与 `/美食图鉴` 删除页码参数，保留品质和未收集筛选，一次读取并展示全部可见条目。
- 图鉴按一至六星稳定分区，每个分区独立显示已收集数和可见总数。
- 群 `qq:1092931381` 可见 83 只猪和 15 道美食，其中 2 个猪槽位与 2 个美食槽位是未发现的保密六星；未授权群不包含这些条目。
- 用户提供的四个定制源文件与当前正式清单逐字段一致，二进制 SHA-256、模板 ID、群授权、同意状态和运行文件均复验通过，因此没有制造重复模板或重复导入。
- Chromium 完整长图：猪图鉴 `1128 x 4782`、`1,583,988` 字节；美食图鉴 `1128 x 2044`、`360,674` 字节。
- 两张图均无文字裁切、元素越界、坏图或空白输出，低于 12 MiB 静态图上限。
- 自动化覆盖无页码解析、超过旧每页数量时完整返回、白名单未知槽位、跨群隔离和品质分区模板。
- pytest `127 passed`，Python 编译、Ruff、离线依赖锁和命令级 UAT 全部通过。
- 运行中 MaiBot 已加载 `v1.1.0`，生产巡检为完整性 `ok`、账本异常 `0`、素材缺失 `0/98`；升级保护备份为 `pig_catcher-pre-v1.1.0-20260728-204536.sqlite3`，Schema `5` 且 `quick_check=ok`。
- 按用户要求未操作真实 QQ；群内实际发送由用户上线后自行体验。

## 14. v1.2.0 商城、美食效果与展示优化验收

验收日期：`2026-07-29`

- 插件：`local.pig-catcher v1.2.0`，阶段 `6`，Schema `6`、Asset Manifest `3`、Ruleset `4`。
- 组件边界：新增 `/升级` 与 `/批量售卖`，共 `29` 个显式 `COMMAND`；仍无普通消息监听、Tool、LLM、网关或 MaiBot 核心修改。
- 正式目录：`102` 项，包含 `83` 只猪、`19` 道美食、`9` 项动画和 `4` 项群专属六星素材；正式包 SHA-256 为 `e3540996cad5b9c1c63d905a58c22fdf93ed6702df2796f1239a210db32e2b6f`。
- 新增美食：四星 `猪咪虾寿司`、`猪猪玉子烧`，五星 `猪寿司拼盘`、`一猪六吃`；原始图片字节和 SHA-256 从用户素材库完整保留。
- 商城：10 种商品在单页完整展示；消耗品通过 `/购买 名称 [数量]` 购买，猪饲料和厨具通过 `/升级 猪饲料|厨具` 从 `Lv.0` 原子升级至 `Lv.5`。
- 背包：猪与美食默认按当前官方价值从高到低排序，每页 `12` 项；GIF/动画 WebP 在背包、图鉴和排行中显示中间代表帧，单体抓取与详情卡仍保留完整动画。
- 低星快捷处理：省略选择器时，做菜、吃菜、售卖会稳定选择最低价值且未锁定的 1 至 3 星资产；`/批量售卖 猪猪|美食` 在一个事务内处理全部符合条件的低星资产。
- 美食效果：`player_food_effects` 持久队列支持抓猪品质、做菜品质、下一次六星做菜、额外抓猪次数、指定品质、巨物和迷你等一次性效果；失败、回滚或重启不会提前消费。
- 六星做菜：基础仍为五星 `90%`、六星 `10%`，只有清单显式声明的 `next-six-star-cook` 可把下一次六星菜概率调整到上限 `35%`，结果仍严格限定为五星或六星。
- 等级：数值等级按累计经验持续增长；荣誉称号独立展示，不暗中提高抓猪或做菜概率。
- 展示：抓猪和做菜结果放大品质标题，并将体型、重量、肥瘦状态改为“迷你/标准/巨物、轻盈/匀称/重量级、精瘦/匀称/圆润”等直观文案。
- Chromium：13 个玩法视图和 11 个经济视图均无裁切、越界或坏图；商城单页为 `1128 x 1953`，完整猪图鉴为 `1128 x 4782`，完整美食图鉴为 `1128 x 2077`。
- 命令级 UAT：34 次图片发送、零文字兜底；重复购买、重复做菜及重启后重复消息均零新增发送；低星自动选择、批量售卖、升级、全部九种高星美食效果和六星边界通过。
- 数据保护：生产升级前备份为 `pig_catcher-pre-v1.2.0-assets-20260729-120402.sqlite3`；备份与升级后数据库均为 `quick_check=ok`。原 `47` 个玩家、`500` 只猪实例和 `78` 道美食实例保持不变，账本异常为 `0`。
- 生产素材：两个白名单群 `qq:1092931381` 与 `qq:237716658` 均可读取各自授权的四项定制素材；正式目录无缺失文件。
- 自动化：pytest `140 passed`；Ruff、Python `compileall`、`uv lock --check` 和 `git diff --check` 通过。
- 运行中宿主：受控重启后 MaiBot 明确加载并注册 `local.pig-catcher v1.2.0`；Runner 汇总 `loaded=20 failed=0 inactive=3`，生产巡检为素材缺失 `0/102`，NapCat 网关持续报告 `online=True good=True`。

已知缺口：

- 用户明确选择在正式使用中自行验证真实 QQ 交互，本轮不把隔离命令 UAT、Chromium 图片或健康网关表述为真实 QQ 已验收。
- 荣誉称号当前按需求仅展示；未来若增加等级权益，必须显式更新帮助、数值规则与 Ruleset。
- 新增高星美食必须在正式清单中显式声明受支持的效果和参数；未知效果会在消耗资产前拒绝。

## 15. v1.2.1 概率调平、帮助精简与当日额度重置

验收日期：`2026-07-29`

- 插件：`local.pig-catcher v1.2.1`，Schema `6`、Asset Manifest `3` 保持不变，Ruleset 升级为 `5`。
- 基础概率：从 `55/25/12/5.5/2/0.5` 调整为 `40/30/17/8/4/1`，总和严格为 100；四至六星合计从 `8%` 提升为 `13%`。
- 无六星素材群：六星的 `1%` 转入五星，实际分布为 `40/30/17/8/5/0`，不读取其他群的私有模板。
- 长期加成：饲料 `Lv.5` 时四至六星合计约 `14.299%`；基础幸运猪哨状态下约 `13.971%`，仍保持六星稀有。
- 帮助：移除版本、开发轮次和“已开放功能”说明；`/抓猪档案`、`/抓猪详情` 不再出现在帮助列表，但两个原命令和全部业务功能均未删除。
- 当日重置：北京时间 `2026-07-29 12:36:54` 写入全局审计事件 `daily-catch-quota-reset-20260729-v121`。此前 `24` 名玩家的 `144` 条今日抓取回执完整保留，有效当日计数变为 `0`。
- 数据保护：重置没有修改实例、猪币、经验、图鉴、纪录或累计统计；生产库仍有 `500` 只猪、`78` 道美食和累计抓取 `500` 次，Schema `6` 且 `quick_check=ok`。
- 备份：重置前在线备份 `pig_catcher-pre-v1.2.1-daily-reset-20260729-123654.sqlite3` 已验证为 Schema `6`、`quick_check=ok`。
- 自动化：pytest `140 passed`；Ruff、Python `compileall`、`uv lock --check` 和 `git diff --check` 通过。
- 运行中宿主：受控重启后加载 `local.pig-catcher v1.2.1`，Runner 汇总 `loaded=20 failed=0 inactive=3`；生产巡检为完整性 `ok`、账本异常 `0`、素材缺失 `0/102`，NapCat 持续报告 `online=True good=True`。

## 16. v1.2.2 新素材、双群授权与当日额度重置

验收日期：`2026-07-29`

- 插件：`local.pig-catcher v1.2.2`，阶段 `6`，Schema `6`、Asset Manifest `3`、Ruleset `5` 均保持不变。
- 正式目录：`112` 项，包含 `88` 只猪、`24` 道美食、`9` 项动画、`6` 项群专属六星素材和 `9` 个 BanG Dream 联动槽位；素材包含 `111` 份唯一二进制。
- Afterglow：新增五星“大绯猪”“摩卡猪”和四星“巴巴猪”“鸫鸫猪”，分别对应上原绯玛丽、青叶摩卡、宇田川巴和羽泽鸫；与原“红挑染猪”组成稳定槽位 `1-5`，完整图鉴显示 `Afterglow 5/5`。
- 六星定制：新增“小马猪”与“小马猪蒙布朗”，使用一份稳定模板分别授权群 `qq:1092931381` 和 `qq:237716658`，没有复制目录条目。
- 公共美食：新增五星“猪猪白菜炖粉条”、四星“一盒油炸猪”、二星“一只油炸猪”和一星“猪肉脯”；高星效果参数均通过 Asset Manifest 导入校验。
- 效果：一盒油炸猪提供 `1` 次额外抓猪机会；猪猪白菜炖粉条为下一次兼容做菜提供 `12` 个百分点提质；小马猪蒙布朗将下一次六星猪权重乘数设为允许上限 `3.0`。
- 原始媒体：十张新图均按内容识别为静态 RGB PNG，最短边不低于 `1080`，单图不超过当前 `12 MiB` 限额；源文件、候选包和运行副本 SHA-256 完全一致，未重编码。
- 隔离：两个授权群各可见 `88` 只猪和 `24` 道美食，其中各有 `3` 个私有猪槽位和 `3` 个私有美食槽位；无关测试群只可见 `85/21`，私有槽位与小马条目均为 `0`。
- 图片：`112` 张正式素材卡全部生成，含 `10` 张联系表与 `9` 张动画逐帧条；两群完整猪图鉴均为 `1128 x 5012`，完整美食图鉴均为 `1128 x 2077`，无文字裁切、越界、坏图或空白输出。
- 发布：生产目录哈希为 `acd308677791f4f53305e3ae945df40cd34a6c31c22da7f6ddcc7cc322fe7f2f`；`112` 个启用模板映射到 `111` 份唯一运行文件，缺失与哈希异常均为 `0`。
- 数据保护：发布前在线备份 `pig_catcher-pre-v1.2.2-assets-and-reset-20260729-170214.sqlite3` 为 Schema `6` 且 `quick_check=ok`，保留 `857` 条抓猪回执、`857` 只猪实例和 `133` 道美食实例。
- 当日重置：写入全局审计事件 `daily-catch-quota-reset-20260729-v122-20260729-171025-115946`；北京时间当日 `501` 条原始回执完整保留，上次重置后的 `357` 次有效计数从两群 `187/170` 归零。
- 不变量：累计抓取仍为 `857`，猪与美食实例仍为 `857/133`，`last_catch_at` 未修改，账本异常为 `0`，数据库 `quick_check=ok`。
- 自动化：pytest `141 passed`；Python 编译、Ruff、Manifest/config 解析、离线依赖锁、全部维护工具 `--help`、隔离导入和运行中宿主加载均通过。
- 运行中宿主：日志确认 `local.pig-catcher v1.2.2` 加载并注册成功；正式目录切换不需要修改 MaiBot 核心代码。

人工项：

- 按用户此前决定，本轮不主动向真实 QQ 群发送测试命令；真实群内抓取、图鉴和食用体验由用户在正常使用中验证。
- Pillow 会对一项既有旧素材报告非阻断的损坏 EXIF 元数据警告；媒体像素、动画帧、正式卡片和运行哈希均已完整验证，本轮十张新 PNG 不受影响。

## 17. v1.2.3 等级概率与升级数值公开验收

验收日期：`2026-07-29`

- 插件：`local.pig-catcher v1.2.3`，阶段 `6`，Schema `6`、Asset Manifest `3`
  保持不变，Ruleset 升级为 `6`。
- 数值等级：`Lv.1` 不加成，每 4 级形成一档，`Lv.21` 达到 5 档封顶；抓猪
  最高等效于单独使用 `Lv.5` 猪饲料，普通做菜高档结果相对权重最高 `+5%`。
- 叠加顺序：数值等级与猪饲料、厨具按相对乘数相乘，再统一归一化；荣誉称号
  不进入概率链路。
- 审计：抓猪与做菜在结算前读取累计经验推导等级，并将 `player_level` 写入各自
  随机快照；历史实例和历史快照不追溯重算。
- 档案：新增“等级概率加成”，显示等级单独作用时的 4 至 6 星合计概率、普通
  做菜高档权重增幅和 `Lv.21` 封顶提示。
- 商城：猪饲料 `Lv.0-5` 的 4 至 6 星合计概率依次显示为
  `13.00/13.27/13.54/13.80/14.05/14.30%`；厨具高档菜相对权重依次显示为
  `+0/+2/+4/+6/+8/+10%`，并高亮当前等级。
- 六星边界：数值等级和厨具不改变六星猪做菜基础 `90% 五星 / 10% 六星`；
  既有清单型一次性六星效果规则不变。
- 固定随机测试：抓猪和普通做菜都覆盖“同一落点在 `Lv.1` 为低档、在
  `Lv.21` 被推到下一档”的真实事务，并验证快照等级为 `21`。
- Chromium：经济视图 `11` 张、抓猪与收藏视图 `13` 张均无裁切、越界或坏图；
  新商城为 `1128 x 2434`，档案为 `1128 x 1161`，480px 缩略图仍可辨认概率值。
- 数据迁移：无；本轮不修改玩家经验、升级等级、资产、猪币、次数或正式素材目录。

人工项：

- 依照用户此前决定，不主动向真实 QQ 群发送验收命令；本轮结论来自纯规则测试、
  隔离命令事务、Chromium 渲染和运行中宿主加载检查。

## 18. v1.2.4 猪纵连、立绘更新与 Poppin'Party 联动验收

验收日期：`2026-07-30`

- 插件：`local.pig-catcher v1.2.4`，阶段 `6`，Schema `6`、Asset Manifest `3`
  和 Ruleset `6` 均保持不变。
- 正式目录：`116` 项，包含 `92` 只猪、`24` 道美食、`9` 项动画、`6` 项群专属
  六星素材和 `12` 个 BanG Dream 联动槽位；素材包含 `115` 份唯一二进制。
- 品质分布：猪为 `20/20/20/17/12/3`，美食为 `2/5/5/5/4/3`。
- 新增三星“猪纵连”；图片为三只首尾相接的小猪队列，正式描述固定为
  “三只小猪首尾相接排成一列，队伍一旦启动就越连越长，谁先掉队谁负责请全队加餐。”
- Poppin'Party：新增四星“兔吉猪”、四星“面包鼓猪”和五星“傲娇猪”，分别映射
  花园多惠 `2/5`、山吹沙绫 `4/5` 和市谷有咲 `5/5`；连同户山香澄 `1/5`、
  牛込里美 `3/5` 后，固定槽位完整为 `{1,2,3,4,5}`。
- 角色依据：联动定义保存 BanG Dream! 官方角色资料 URL；描述结合素材中可见的蓝色
  吉他与兔子、鼓与面包、键盘与盆栽，以及角色的吉他手、鼓手和键盘手设定重新撰写，
  未照抄官方介绍。
- 立绘替换：五星“红挑染猪”“粉音猪”“红茶猪”保留原模板 ID、品质、描述、
  联动归属和历史收藏，只更新原始 PNG 字节；七张目标图的生产文件哈希均与用户提供
  的临时素材一致。
- 素材验收：`116` 张正式素材卡全部生成，含 `10` 张联系表和 `9` 张动画逐帧条；
  七张目标卡均为 `1128 x 820`，描述、星级和联动标签无裁切。
- 综合视图：抓取与收藏 `13` 张、做菜与经济 `11` 张、社交与排行 `8` 张均通过
  Chromium 诊断，无文字裁切、越界或坏图；单猪动画仍保留完整帧序列。
- 完整图鉴：两个授权群均可见 `92/24`，猪图鉴为 `1128 x 5242`，美食图鉴为
  `1128 x 2077`；未授权群仅可见 `89/21`，分别为 `1128 x 4960` 与
  `1128 x 1795`。三种视图均无裁切、越界、坏图或空白输出。
- 发布：生产目录以哈希
  `2b29a9a86381ec24ab92056741a9f505946ce6df4846bdec86bd351efed73570`
  原子激活，`116` 个启用模板、SQLite `quick_check=ok`，七张目标媒体的数据库哈希
  与文件哈希全部一致。
- 数据保护：发布前在线备份为
  `backups/pig_catcher-pre-v1.2.4-assets-20260730-115308.sqlite3`，源库与备份均为
  Schema `6`、完整性 `ok`；旧素材目录和历史资产未删除。
- 工程回归：pytest `146 passed`，`compileall`、Ruff、`uv lock --check` 和
  `git diff --check` 全部通过；运行中 Runner 已成功加载 `local.pig-catcher v1.2.4`。

人工项：

- 依照此前约定，本轮未主动向真实 QQ 群发送测试命令；素材已经在线发布，后续抓取、
  图鉴和收藏体验由用户在正常使用中验证。
- Pillow 仍会对一项既有旧素材报告非阻断的损坏 EXIF 元数据警告；本轮七张 PNG
  均完整解码，像素、哈希和正式卡片不受影响。

## 19. v1.2.5 图鉴 RPC 修复、22 次上限与当日额度重置

验收日期：`2026-07-30`

- 插件：`local.pig-catcher v1.2.5`，阶段 `6`，Schema `6`、Asset Manifest `3`
  保持不变；每日抓猪上限由 `20` 提升为 `22`，成功后冷却仍为 `20` 秒，
  Ruleset 升级为 `7`。
- 根因：完整图鉴把大尺寸源图逐张编码为 Base64 并嵌入 `html2png` 请求，宿主尚未
  开始截图时，RPC 帧已达到 `19,321,289` 字节，超过 `16,777,216` 字节上限；
  最终 PNG 本身不是超限对象。
- 修复：背包、图鉴和排行的紧凑列表媒体改为最长边 `256px`、质量 `82` 的 WebP
  预览；静态图取首帧，动画取确定性的中间帧。源文件不重编码、不覆盖，抓取卡、
  详情卡和动画逐帧合成仍读取原始媒体。
- 防护：HTML 按 UTF-8 计算请求体，在 `12 MiB` 时于插件侧抛出明确错误，预留
  RPC 信封开销并避免再次依赖宿主的模糊超限异常。
- 完整图鉴：授权群猪图鉴请求为 `837,436` 字节，输出
  `1128 x 5242 / 1,801,172` 字节；美食图鉴请求为 `277,295` 字节，输出
  `1128 x 2077 / 597,023` 字节。与原失败请求相比，猪图鉴 HTML 减少约 `95.7%`，
  且两个授权群共四张完整长图均无裁切、越界、坏图或空白输出。
- 综合视图：抓取与收藏 `13` 张、做菜与经济 `11` 张、社交与排行 `8` 张全部通过
  Chromium 诊断；单体图片清晰度、动画帧数、有效时长、循环和原始文件哈希保持不变。
- 自动化：pytest `148 passed`；Ruff、Python `compileall`、`uv lock --check`、
  Manifest/config 解析和 `git diff --check` 均通过。
- 生产备份：额度重置前在线备份
  `backups/pig_catcher-pre-v1.2.5-render-quota-reset-20260730-193823.sqlite3`
  为 Schema `6`、`quick_check=ok`，大小 `9,076,736` 字节。
- 当日重置：北京时间 `2026-07-30 19:38:23` 写入全局审计事件
  `daily-catch-quota-reset-2026-07-30-v125-20260730-193823-8079a59b`。重置前
  `57` 名玩家共有 `585` 次有效抓取，两群分别为 `276/309`；当日原始
  `1,000` 条抓猪回执全部保留，有效计数归零。
- 不变量：重置事务没有修改 `2,936` 条命令回执、`2,186` 只猪、`362` 份美食、
  `2,841` 条猪币账本、累计抓取 `2,186` 次或任一 `last_catch_at`；生产库提交后
  `quick_check=ok`。
- 运行中宿主：日志确认 `local.pig-catcher v1.2.5` 加载并注册成功，Runner 汇总
  `loaded=20 failed=0 inactive=3`，生产巡检完整性 `ok`、账本异常 `0`、素材缺失
  `0/116`；NapCat 持续报告 `online=True good=True`。重置后的真实群抓取回执已显示
  `今日抓猪：1/22`，证明新额度与全局重置同时生效。

人工项：

- 本轮没有由 Codex 再向真实 QQ 群主动发送完整图鉴命令；完整图鉴使用正式目录、
  两个授权群上下文和真实 Chromium 完成同路径验收，运行中的下一次图鉴请求会使用
  已加载的紧凑预览实现。

## 20. v1.2.6 群素材强隔离与六星对应菜验收

验收日期：`2026-07-31`

- 插件：`local.pig-catcher v1.2.6`，阶段 `6`，Schema `7`、Asset Manifest `4`、
  Ruleset `8`。
- 正式目录：`122` 项模板，包含 `95` 只猪、`27` 道美食、`9` 项动画和 `12` 项
  群专属模板；因两个群的 6 份内容相同，内容哈希仍为 `115` 种，跨群分别存储后共有
  `121` 份媒体文件。
- 物理隔离：`定制猪群友库` 和 `定制美食库` 均包含两个独立群号子目录，每个目录
  分别有 `3` 只猪和 `3` 道菜；六对同名文件当前 SHA-256 相同，但 Manifest 和运行
  路径均指向各自群号目录。
- 逻辑隔离：两个群分别使用独立的 3 个猪模板和 3 个美食模板；每群可见范围保持
  `92/24`，每群授权映射均严格为 `3/3`，不相关群仍只看到 `89/21` 公共模板。
- 对应菜：1004猪鼻哥只对应猪鼻蛋包饭，撅撅猪只对应撅撅猪派，小马猪只对应
  小马猪蒙布朗；Manifest 4 拒绝缺失、跨群、非六星、重复和未完整覆盖的绑定。
- 事务结算：六星猪的品质结果仍为基础 `90%` 五星、`10%` 六星；六星结果只读取
  `paired_food_template_id`，绑定不可用时原料、道具和美食效果全部不消费。
- 生产迁移：第二群的 `14` 只历史六星猪实例、`13` 个猪图鉴条目、`6` 个品种纪录、
  `4` 个巨物目击、`1` 道六星菜实例和 `1` 个美食图鉴条目无损改绑到该群新模板；
  历史数量、所有者、状态、数值、`5,064` 条命令回执和 `4,879` 条账本均未改变。
- 生产发布：目录哈希
  `79eaadcf3590d9761d262806e2cf6fb21c3b45ab9fbbac5d2bc24fb526399243`
  已激活；`122` 个启用模板媒体全部存在且 SHA-256 相符，`quick_check=ok`，
  `foreign_key_check=0`。
- 数据保护：发布前 SQLite 在线备份为
  `backups/pig_catcher-pre-v1.2.6-group-assets-20260731-100859.sqlite3`，
  大小 `15,269,888` 字节，Schema `6` 且 `quick_check=ok`；旧本机素材包另存于
  `artifacts/backups/asset-library-current-pre-v1.2.6-20260731-100859`。
- 隔离结算验收：在迁移后的生产库克隆中，第一群“撅撅猪”固定产出“撅撅猪派”，
  第二群“1004猪鼻哥”固定产出“猪鼻蛋包饭”，克隆库完整性仍为 `ok`。
- 工程回归：pytest `152 passed`，Ruff、Python `compileall`、依赖锁与
  `git diff --check` 通过。

人工项：

- 本轮未启动原本处于停止状态的 MaiBot，也未主动向真实 QQ 群发送测试命令；代码、
  Schema 7、正式目录和生产数据均已准备好供下一次正常启动加载。

## 21. v1.3.0 分时额度、精准重置、六星强化与概率公开验收

验收日期：`2026-07-31`

- 插件：`local.pig-catcher v1.3.0`，阶段 `6`，Schema `7`、Asset Manifest `4`，
  Ruleset `9`。
- 分时额度：北京时间 `00:00`、`09:00`、`12:00`、`19:00` 分别开启独立时段，
  每群每人每个时段有 `5` 次基础抓取；成功抓取后的 `20` 秒冷却只在当前时段内生效，
  自然换段或成功执行手工重置都会清除上一时段或重置前的冷却基准。
- 精准重置：`tools/reset_catch_quota.py --group-id <精确群号>` 与 WebUI
  “重置抓猪次数”管理卡均只重置指定群当前时段。执行前使用 SQLite 在线备份，
  执行后写入带群范围的审计事件；不删除命令回执、实例、猪币、经验、图鉴、纪录或
  累计统计。WebUI 一次性开关成功后自动关闭，未知群号在备份和写入前拒绝。
- 六星收益：新六星菜基础价值 `25,000` 猪币，做菜奖励 `1,500` 猪币与 `800`
  经验，食用奖励 `1,200` 经验。撅撅猪派的迷你效果强度提升到 `0.50`，
  猪鼻蛋包饭把下一次六星猪做出六星菜的概率提升到 `60%`，小马猪蒙布朗把下一次
  指定六星猪权重放大到 `12` 倍；历史实例与历史效果队列不追溯重算。
- 商城概率：幸运猪哨完整公开使用前后概率，依次为
  `40.00→38.65 / 30.00→28.99 / 17.00→18.40 / 8.00→8.66 /
  4.00→4.33 / 1.00→0.99%`；主厨香料按 `1-5` 星原料逐行公开使用前后
  做菜品质分布，并明确其规则是从最低可出档向高一档转移最多 `6` 个百分点。
- 生产配置：已解析确认 `config_version=1.3.0`、每时段 `5` 次、刷新时刻
  `[0, 9, 12, 19]`、冷却 `20` 秒，管理重置触发器保持关闭且未填写目标群，
  因此本轮没有执行生产额度重置。
- 生产目录：目录哈希
  `588677270076af75ed4f40919319ffb2e742df708941debff301d29e67064631`
  已激活；共 `122` 个条目、`95` 个猪模板、`27` 个美食模板，两类群授权映射均为
  `6`，`quick_check=ok`，`foreign_key_check=0`。
- 数据保护：发布前 SQLite 在线备份为
  `backups/pig_catcher-pre-v1.3.0-quota-and-six-star-20260731-1507.sqlite3`，
  大小 `15,273,984` 字节，Schema `7` 且 `quick_check=ok`；备份保留
  `5,064` 条命令回执、`3,726` 只猪、`648` 道美食和 `9` 条审计事件。
- 视觉验收：抓猪与收藏 `13` 个视图、做菜与经济 `11` 个视图、社交与排行 `8`
  个视图全部通过 Chromium 检查，零文字裁切、零元素越界、零坏图；商城长图为
  `1128 x 2946`。
- 工程回归：pytest `158 passed`，Ruff、Python `compileall`、`uv lock --check`
  和 `git diff --check` 全部通过。

人工项：

- 本轮未启动原本处于停止状态的 MaiBot，也未向真实 QQ 群发送测试命令；生产文件、
  配置与数据已准备好供下一次正常启动加载。

## 22. v1.3.1 幸运猪哨概率强化验收

验收日期：`2026-07-31`

- 插件：`local.pig-catcher v1.3.1`，Schema `7`、Asset Manifest `4` 保持不变，
  Ruleset 升级为 `10`。
- 幸运猪哨：无其他加成时，六档概率从 `40/30/17/8/4/1%` 精确调整为
  `38/29/16.5/10/5/1.5%`；六档合计严格为 `100%`。
- 变更边界：只调整幸运猪哨概率。主厨香料、道具价格、抓猪次数、冷却、六星菜、
  素材目录、数据库结构和历史随机快照均未改变。
- 展示验收：商城图片与文字兜底均从领域概率函数读取新结果；商城长图
  `1128 x 2946`，Chromium 诊断为零文字裁切、零元素越界、零坏图。
- 工程回归：pytest `158 passed`，Ruff、Python `compileall`、`uv lock --check`
  和 `git diff --check` 全部通过。

人工项：

- 本轮未启动 MaiBot、未发送真实 QQ 命令，也未执行任何额度重置。

## 23. v1.3.2 管理员重置与额度显示优化

- 新增群内 `/重置`：仅“插件管理员”列表中的当前平台用户可执行，命令固定作用于消息
  所在群；执行前在线备份，结算写入群级审计事件和幂等命令回执，不删除历史数据。
- 管理员身份支持原始用户 ID 和 `platform:user_id`；NapCat 可使用数字 QQ 号，QQ 官方
  机器人使用成员 OpenID，避免把无法取得的数字 QQ 号作为安全判断依据。
- 次数分母改为“基础额度、本窗口已用次数与当前剩余额外次数”的有效上限；日内换段或
  手动重置后，已在旧窗口消耗的额外次数不再错误显示为可用额度。
- 本轮不改变额外次数的发放、消费和到期规则，不修改 Schema、素材、概率、价格或奖励。

## 24. v1.4.0 素材体型、超级道具与高星菜强化验收

- 验收日期：`2026-08-04`。生产已加载 `local.pig-catcher v1.4.0`、Schema `8`、
  Ruleset `11`；QQ 官方适配器已加载 `v0.2.1`，两套 Bot 网关均以各自身份启动。
- 150 份素材逐项复核，只有画面、名称或描述明确表达巨型、迷你或扁平的条目覆盖默认品质区间；普通体型继续沿用现有阈值，历史实例不重算。
- 幸运猪哨与主厨香料强化并涨价；新增两种 2000 猪币以上超级道具，同动作装备互斥、不兼容动作不消耗、六星概率食物效果优先于超级香料。
- 五星菜排除“一猪六吃”和额外次数菜后完成大幅增强；六星菜覆盖精确 50% 六星抓取、最高 60% 六星做菜、周全时段 +5 与永久全时段 +1（上限 +5）。
- Schema 8 对周额度与永久额度单独持久化；周加成不可叠加且按北京时间下周一零点到期，永久上限拒绝消费并回滚整笔品鉴。
- 正式模板 ID、群六星一对一配菜、附加官方群授权、旧资产快照和历史回执保持不变。
- 正式目录哈希 `446bc971504360dc692fd90c3297f0ff3efdc8e0d285b6c8def920f189ea5e38`
  已激活：109 只猪、41 道美食；4 个授权群作用域均各有 5 只六星猪和 5 道
  六星菜。生产数据库 `quick_check=ok`、外键违规为 `0`。
- 发布前在线备份为
  `backups/pig_catcher-pre-v1.4.0-assets-20260804-110333.sqlite3`，大小
  `27,369,472` 字节，Schema `8`、`quick_check=ok`、外键违规为 `0`。
- 工程回归：抓猪插件 pytest `167 passed`，QQ 官方适配器 pytest `46 passed`；
  两边 Ruff、Python `compileall`、`uv lock --check` 与 `git diff --check` 全部通过。
- 隔离 UAT：做菜/经济完整指令链通过；抓猪/收藏验证授权群六星、普通群五星
  封顶、道具单次消费、重启幂等与动画投递。商城 12 个商品长图和 150 项素材审计图
  均通过零裁切、零越界、零坏图检查。

## 25. v1.4.1 Pastel＊Palettes 联动与彩彩修车定制验收

- 验收日期：`2026-08-04`。Pastel＊Palettes 新增五只联动猪并固定为 `5/5`：
  丸山彩、冰川日菜、白鹭千圣、大和麻弥、若宫伊芙分别占用槽位 `1-5`，品质按需求为
  `5/5/4/4/5` 星。
- 角色描述以 BanG Dream! 官方资料与本批素材画面为依据；“彩彩修车”只按玩家社区
  谐音和联机房用语解释，并明确不是丸山彩的官方职业设定。
- 新增两套群隔离的六星彩彩修车猪及对应六星彩彩修车猪慕斯；慕斯沿用
  `weekly-window-catches` 效果，本周每个抓猪时段 `+5` 次且不可重复叠加。
- 原有 29 个命令和帮助文本未修改，`/重置` 不进入 `/抓猪帮助`。
- 正式素材包为 159 项模板、146 份唯一二进制、158 份隔离存储媒体；Schema `8`、
  Asset Manifest `4`、Ruleset `11` 保持不变。
- 正式目录哈希为
  `d99d71f6c9b2b80d8e6768e619cd3356c9474d83e4061490577146e3102c715a`；
  生产已激活 116 只猪和 43 道美食，4 个既有授权群作用域均各有 6 只六星猪和
  6 道六星菜。数据库 `quick_check=ok`、外键违规为 `0`。
- 发布前在线备份为
  `backups/pig_catcher-pre-v1.4.1-pastel-palettes-20260804-114902.sqlite3`，大小
  `27,500,544` 字节，Schema `8`、`quick_check=ok`、外键违规为 `0`。
- 生产与备份逐表对比后，22 张玩家与业务数据表行数未变化；只新增素材导入记录、
  7 个猪模板、2 个美食模板及各 4 条群授权映射。
- 工程回归为 pytest `167 passed`；Ruff、Python `compileall`、`uv lock --check`
  与 `git diff --check` 全部通过。159 项卡片、14 张联系表及两套完整猪/美食图鉴均
  为零裁切、零越界、零坏图；运行日志确认 `local.pig-catcher v1.4.1` 已加载，
  生产导入后未出现插件错误或警告。

## 26. v1.4.2 永久社交黑名单与一周额度处罚验收

- Schema `10` 提供三种独立限制：`gift-transfer-ban`、`trade-ban` 和
  `catch-window-limit`；前两种不设到期时间，后一种必须记录明确到期时间。
- 赠送与收赠检查赠送黑名单，创建与接受交易检查交易黑名单；任一参与方命中即整笔
  拒绝。批量处罚同时取消相关待处理报价并释放锁，不删除任何既有资产或统计。
- 抓猪处罚在每个北京时间刷新时段都把最终额度限制为指定值，并覆盖全部额度加成；
  到期后自动恢复正常额度，永久社交黑名单保持不变。
- `tools/apply_player_restrictions.py` 在写入前在线备份生产库，要求群号唯一命中既有
  作用域、所有用户 ID 均属于该群，并在同一事务写入限制与审计事件。

## 27. v1.4.3 Hello, Happy World! 联动与保千猪表情包验收

- 验收日期：`2026-08-05`。插件升级为 `local.pig-catcher v1.4.3`、Schema `12`、
  Asset Manifest `4`、Ruleset `12`。
- 新增六只 BanG Dream! `Hello, Happy World!` 联动猪：五星 HAPPY 猪（弦卷心）、
  五星歌剧猪（濑田熏）、五星迷路猪（松原花音）、四星美咲猪（奥泽美咲）、
  四星可乐饼猪（育美）和三星米歇尔猪（米歇尔）。前五只占用固定槽位 `1-5`，
  米歇尔猪作为乐队吉祥物熊单槽独立，不参与 `5/5` 计数。
- 描述词结合官方设定与素材画面重新撰写，角色资料 URL 均来自
  `https://bang-dream.com/` 或 `https://anime.bang-dream.com/`。
- 新增两套群隔离的六星保千猪及对应六星定制菜“猪保千猪排轮盘”；保千猪描述词
  按用户提供文案固定为“因为猪保千，现在保千猪的信誉分已经不够刷共享单车赶去加餐了”。
- 保千猪新机制：抓到保千猪前会先发送猪保千表情包，再渲染并发送猪猪卡片；
  玩家输入 `/切换 猪保千` 可在已拥有的保千猪实例立绘与表情包之间切换，
  `display_variant` 持久保存。
- 数据结构：`pig_templates` 新增 `alternate_image_relpath`，
  `pig_instances` 新增 `display_variant`；新增 `v0011` 与 `v0012` 迁移，
  兼容旧版 v9 测试库的最小表结构补全。
- 正式目录：`169` 项模板，包含 `124` 只猪、`45` 道美食、`12` 项动画、
  `28` 项群专属；素材包包含 `155` 份唯一二进制和 `170` 份隔离存储媒体。
- 两个授权群 `qq:1092931381` 与 `qq:237716658` 各自可见 `117` 只猪和
  `38` 道美食；未授权群仅可见公共模板。
- 工程回归：pytest `169 passed`；Ruff、Python `compileall`、
  `uv lock --check` 与 `git diff --check` 全部通过。
- 运行中宿主：日志确认 `local.pig-catcher v1.4.3` 加载并注册成功；
  生产巡检完整性 `ok`、账本异常 `0`、素材缺失 `0/169`。

人工项：

- 按用户此前决定，本轮未主动向真实 QQ 群发送测试命令；素材与命令已在线发布，
  真实群内的保千猪表情包、切换与图鉴体验由用户在正常使用中验证。

## 28. v1.5.0 全量审计与运营控制面板验收

- 验收日期：`2026-08-06`。插件升级为 `local.pig-catcher v1.5.0`，Schema `12`、
  Asset Manifest `4`、Ruleset `12` 保持不变，旧数据不需要迁移。
- 首页入口升级为“抓猪运营管理”；额度重置、赠送/交易黑名单和群公告均采用保存后执行的
  一次性开关，处理器先关闭开关再产生副作用，配置重载或重启不会自动重复处罚或公告。
- 黑名单面板支持对赠送/收赠与交易两类永久限制独立加入或解除；目标群和全部成员必须
  精确命中既有数据，变更前在线备份，整批原子提交，并取消新交易黑名单成员的待处理报价。
- 公告通过目标群最近 `stream_id` 调用 SDK 文字发送，领取、成功和失败均写审计，失败
  不自动重试。QQ 官方主动消息权限或约五分钟被动回复上下文仍是平台侧必要条件。
- 全量审计修复了两个隐藏问题：重复抓猪回执会重复发送备用表情包；备用图片未执行与主图
  一致的安全、格式、逐帧和哈希校验。备用图哈希现在同时参与素材目录哈希。
- 目录整理后，`asset_library/current/` 是当前包，历史包进入忽略的 `archive/`，本机一次性
  运维脚本进入忽略的 `tools/local/`，历史说明归档到 `docs/references/`。
- 自动回归为 pytest `178 passed`；Ruff、Python `compileall` 与 `uv lock --check`
  全部通过。为避免真实群副作用，公告与处罚使用离线 SDK/SQLite 隔离测试，不主动向群发送。
- 当前素材包以 200px 兼容阈值严格复验：169 项模板、12 项动画、2 张备用图全部通过，
  新目录哈希为 `53f9f132a796105d996ac508331e0b0dbeef90d6bcb66f1ccb11b6f3d2555e49`。
- 生产数据库只读核验：Schema `12`、`quick_check=ok`、外键违规 `0`，124 个猪模板、
  45 个美食模板；运行日志确认 `local.pig-catcher v1.5.0` 注册成功，巡检账本异常 `0`、
  素材缺失 `0/169`。
- 生产数据库备份副本的恢复 UAT 共 12 步通过：图片失败文字降级、重复抑制、缺图占位、
  SQLite 写锁回滚、重启后报价解锁、双群隔离、全库账本对账和干净恢复均成功；恢复库
  `quick_check=ok`。社交与排行隔离 UAT 同步通过，赠送/成交事件各 1、成交账本两条零和、
  七类排行和重启幂等正常。
- 审计同时修复了 UAT 工具自身的旧组件计数、全库转移事件误计数及 Windows GBK 输出
  问题。所有演练只修改 `artifacts/` 内的数据库副本；真实群公告和处罚仍未触发。

## 29. v1.5.1 做菜冷却、联动保护与批量做菜验收

- 验收日期：`2026-08-07`。插件升级为 `local.pig-catcher v1.5.1`，Schema `12`、
  Asset Manifest `4`、Ruleset `12` 保持不变，旧数据不需要迁移。
- 做菜与批量做菜共用 `cook_cooldown_seconds`（默认 10 秒）冷却，基于
  `player_statistics.last_cook_at` 判断，冷却中抛出 `CookCooldownError` 并提示剩余秒数。
- 米歇尔猪加入 Hello, Happy World! 联动收藏（槽位 6/6），HHW 收集从 5 槽扩为 6 槽；
  素材目录同步重建，新目录哈希
  `7176eb8675e45a684c5ba271cff55dd0eabd9cef3b00eb4a118bdd70ef04039f`。
- 批量售卖与批量做菜在 SQL 层排除 `collection_id` 非空的全部联动猪；`/批量售卖 猪猪`
  支持 `一星` 至 `五星` 品质过滤，`/批量做菜` 支持可选品质且跳过六星定制猪。
- 选择器匹配改为“精确优先 + 去空白忽略英文大小写兜底”，覆盖售卖、赠送、交易、做菜
  与批量操作；“白吃 Token 的猪”等含空格英文名不再要求逐字输入。
- 批量做菜使用多菜合一模板 `batch_cook.html`，产出按品质降序展示，一次渲染不再逐菜出图；
  单次批量做菜不消耗装备道具。
- 自动回归为 pytest `183 passed`；Ruff、Python `compileall` 全部通过。命令组件从
  32 增至 33，`/批量做菜` 已注册并可被帮助命令检索。

## 30. v1.6.0 菜品与道具体系审计重构验收

- 验收日期：`2026-08-07`。插件升级为 `local.pig-catcher v1.6.0`，Schema `12`、
  Asset Manifest `4`、Ruleset `12` 保持不变，旧数据不需要迁移。
- 全量审计 45 道美食：29 道带效果、16 道基础经验；修复 6 处不同名菜同效果问题
  （做菜品质转移、指定品质加权、6 星抓猪概率、额外次数、周额度），调整 12 道菜参数后
  不同名菜效果签名全部唯一，群专属双群复制保持同菜同效果。
- 效果互斥重构：`CATCH_PROBABILITY_GROUP`、`CATCH_STATURE_GROUP`、`COOK_PROBABILITY_GROUP`
  三组；抓猪概率组与做菜概率组内一次动作最多生效最早的一个，其余记入
  `skipped_summaries` 并在结果中提示“互斥未叠加”；体型组与概率组正交可并存。
- 菜品与道具、等级加成可共用：最终概率由基础权重依次叠加饲料/厨具、数值等级、
  道具（唯一装备）与互斥选一的菜品效果后归一化得出。
- 概率展示落地：抓猪卡片与文字摘要展示六档最终概率；做菜卡片与摘要展示成品品质概率
  与最终概率行；均标注来源（等级、饲料/厨具、道具、美食加成）与互斥未叠加项。
- 素材目录重建导入：新目录哈希
  `b10da3ad0252cfc6fb367106d4594ac7f6f3f4849fc054ab38b64ed87f03280f`，
  运行库 `food_templates.effect_params_json` 已更新（如白菜炖粉条 shift 14、猪饺 3 星 ×3）。
- 自动回归为 pytest `185 passed`（新增 2 个互斥分组测试）；Ruff、Python `compileall`
  全部通过。

## 31. v1.6.1 Roselia 联动与 297 期名场面定制验收

- 验收日期：`2026-08-07`。插件升级为 `local.pig-catcher v1.6.1`，Schema `12`、
  Asset Manifest `4`、Ruleset `12` 保持不变，旧数据不需要迁移。
- 新增 Roselia 五只联动猪：歌姬猪（凑友希那，5★）、妈妈猪（今井莉莎，5★）、
  魔王猪（宇田川亚子，5★）、薯条猪（冰川纱夜，4★）、宅宅猪（白金燐子，4★），
  收藏组 `bandori-roselia` 槽位 1-5、进度 `5/5`，全部使用官方角色资料页。
- 新增六星定制猪 ob一串猪 与六星定制美食 糖醋排骨（297 期活动结活名场面），
  在 `qq:1092931381` 与 `qq:237716658` 两个已授权群均可见；糖醋排骨效果为
  下一次抓猪 4 至 6 星相对权重 `×3.0`（参数唯一）。
- 模板总数 169 → 178（猪 124 → 131、菜 45 → 47）；群专属条目 28 → 32
  （每群 8 猪 + 8 菜）；每个授权群可见 123 只猪、39 道菜。
- 素材目录重建导入：178 项模板、162 份唯一内容、179 份隔离存储媒体，
  新目录哈希 `c503edfc2a20ca09252d09d7863e3e4ee4dfb3f16d72f5d91ada791e828c16ec`。
- 自动回归为 pytest `185 passed`；Ruff、Python `compileall` 全部通过。

## 32. v1.6.2 QQ 官方双 Bot 群内容同步验收

- 验收日期：`2026-08-07`。插件升级为 `local.pig-catcher v1.6.2`，Schema `12`、
  Asset Manifest `4`、Ruleset `12` 保持不变，旧数据不需要迁移。
- 根据 `local.qq-official-adapter` 运行数据，主机器人（`2682203558304385787`）最近群
  openid `5E5854406D0297D6FEAE696A13E3A339`，第二机器人（`12855103089515654682`）
  最近群 openid `9EA2810F378FBD7DC3219C56CEAB3520`。
- 为上述两个 `qq-official:<openid>` 数据域新增 ob一串猪 与糖醋排骨（各 1 猪 1 菜），
  模板总数 178 → 182；Roselia 联动为公共素材，QQ 官方群自动可见。
- `template_id` 内 openid 采用小写以满足 `^[a-z0-9]+(?:-[a-z0-9]+)*$` 命名约束，
  `group_scope_id` 保留官方原值（全大写 openid）。
- 素材目录重建导入：182 项模板、162 份唯一内容、183 份隔离存储媒体，
  新目录哈希 `4655105bae94b7973b224c9048f6f99436ba89f3cdbac94a46f9cacdcae100b3`。
- 自动回归为 pytest `185 passed`；Ruff、Python `compileall` 全部通过。

## 33. v1.6.3 六星菜效果重做与重置额度机会验收

- 验收日期：`2026-08-07`。插件升级为 `local.pig-catcher v1.6.3`，Schema `12`、
  Asset Manifest `4`、Ruleset `12` 保持不变，旧数据不需要迁移。
- 六星菜效果全部改为独立生效：新增 `EXCLUSIVE_CATCH_EFFECTS`/`EXCLUSIVE_COOK_EFFECTS`，
  激活独占效果时本次抓猪/做菜跳过装备道具（不消耗、保留），权重从纯基础计算；互斥组约束
  同时保证与其他菜品效果不叠加。
- 新增效果类型：`next-high-star-catch`（固定高星分布 uses）、`next-five-star-cook`
  （必出五星菜 uses）、`even-catch-distribution`（六档均等 uses）、
  `exclusive-catch-quality`（糖醋排骨独占加权）、`quota-reset`（重置机会）；
  多 uses 效果复用 `player_food_effects.granted_uses` 逐次扣减。
- 小马猪蒙布朗 6 星概率 50→60（上限同步 50→60）；雾蓝键盘大福、彩彩修车猪慕斯、
  猪保千猪排轮盘、糖醋排骨效果按运营要求重做。
- 新增 `/重置额度` 命令（组件 33→34）与 `CatchQuotaResetService.reset_from_quota_chance`：
  单事务内校验效果、备份、重置、审计并消耗一次机会；无机会时给出明确提示。
- 素材目录重建导入：新目录哈希
  `a9b9b5b27d329d48502de20aa15ece53118fbbcfd6463d15b06e704042a8d720`。
- 自动回归为 pytest `188 passed`（新增独占效果、重置机会领域/命令测试）；Ruff、
  Python `compileall` 全部通过。

## 34. v1.6.4 多次数做菜效果与批量做菜互斥验收

- 验收日期：`2026-08-07`。插件升级为 `local.pig-catcher v1.6.4`，Schema `12`、
  Asset Manifest `4`、Ruleset `12` 保持不变，旧数据不需要迁移。
- `EconomyService.batch_cook` 在事务内检查玩家有效果队列：存在 `COOK_PROBABILITY_GROUP`
  且剩余次数大于等于 1 的效果时抛出 `BatchCookRestrictedError`，提示“只能逐个使用 /做菜，
  不能批量做菜”；效果剩余 1 次时仍然禁止，直到剩余次数用尽。
- 通用判定（效果分组 + 剩余次数）保证未来新增的多次数做菜六星菜效果自动适用，
  无需逐效果枚举。新增端到端测试：持有 5 次必出五星效果拒绝批量做菜，消耗至 1 次后放行。
- 糖醋排骨吃菜结果（图片 note 与文字摘要）展示“可发送 /重置额度 使用”；无机会时
  /重置额度 也会提示获取途径。自动回归为 pytest `189 passed`；Ruff、compileall 通过。
## 35. v1.6.6 群级窗口提额与官方双群 15 次提额验收

- 验收日期：`2026-08-07`。插件升级为 `local.pig-catcher v1.6.6`，Schema `12 → 13`
  （新增 `quota_window_boosts` 表，主键 `scope_id + window_start`），Asset Manifest
  `4`、Ruleset `12` 保持不变；迁移只做加法，旧数据零改动。
- 机制：`CatchQuotaResetService.apply_window_boost` 在单事务内完成作用域校验、在线备份、
  写入提额记录（`limit_value` 1–1000）、按 `catch-quota-window-boost` 归零窗口用量并审计；
  可一次指定多个作用域。提额记录绑定窗口起始时间，刷新小时 `0/9/12/19` 切换后自动失效，
  无需定时器即可恢复到每时段基础额度。
- 生效规则：`GameplayService.catch` / `profile` 命中当前窗口提额记录时，本时段额度按提升值
  计算，并暂时忽略 `catch-window-limit` 违规限制（违规者在提额窗口内同样可抓满提升额度；
  限制记录不删除，窗口结束后自动恢复）。随机快照新增 `quota_window_boost_limit` 字段。
- 运营面板：`QuotaAdministrationSection` 新增“平台（可选）”与“提额度数”，支持精确操作
  `qq` / `qq-official` 群并执行提额。
- 运营执行（`source=manual-2026-08-07`）：`qq-official:5E5854406D0297D6FEAE696A13E3A339`
  与 `qq-official:9EA2810F378FBD7DC3219C56CEAB3520` 两个官方群当前窗口
  （2026-08-07 12:00–19:00）提额至 15 次/时段；清空 409 次用量、涉及 49 名玩家，
  含四名违规者（千早の花火、纯良小白、软糯丰川祥、xc，其中 xc 限制 1 次至 8/8）。
  备份：`backups/pig_catcher-pre-quota-reset-multi-72593c8b17f7-20260807-071432.sqlite3`；
  审计事件 `cbe5e669…`、`d6fc91f3…`。19:00 窗口切换后全量自动恢复每时段 5 次。
- 自动回归为 pytest `191 passed`（新增提额端到端测试与面板一次性提额测试）；Ruff、
  Python `compileall` 全部通过。
## 36. v1.6.7 六星内容四群全量开放与跨群授权修复验收

- 验收日期：`2026-08-07`。插件升级为 `local.pig-catcher v1.6.7`，Schema `13`、
  Asset Manifest `4`、Ruleset `12` 保持不变；旧数据不需要迁移。
- 目录新增：两个 `qq-official:<openid>` 数据域各 7 猪 + 7 菜（NapCat 双群六星
  定制内容的官方群副本，`template_id` 前缀 `qo<openid>`，`paired_food_template_id`
  同域配对），模板总数 182 → 210；全部 8 道六星菜（含糖醋排骨）在四个群可抓、可做、可吃。
- 修复 v1.6.2 遗留跨群授权 bug：官方群 scope 曾错误持有 NapCat `qq:237716658`
  的 7 猪 + 7 菜授权（在线备份 `pre-sixstar-cleanup-20260807-152540` 佐证）。
  一次性修正脚本清理 28 条脏授权，并把官方群已持有的 118 只实例、129 条图鉴
  记录迁移合并到官方群 `qo` 模板（图鉴合并 acquired_count/best_size/best_weight，
  first/last 取极值）；执行后孤儿实例/图鉴为 0。
- 素材目录重建导入：210 项模板、162 份唯一内容、213 份隔离存储媒体，
  新目录哈希 `976761ab88…`，运行库 catalog hash `69c2dc82…`。
- 自动回归为 pytest `191 passed`；Ruff、Python `compileall` 全部通过。
## 37. v1.6.8 批量保留开关与批量做菜低星范围验收

- 验收日期：`2026-08-07`。插件升级为 `local.pig-catcher v1.6.8`，Schema `13 → 14`
  （`players.batch_keep_highest` 玩家偏好列），Asset Manifest `4`、Ruleset `12`
  保持不变；迁移只做加法，旧数据零改动。
- 保留规则：联动猪默认只保留一只价值最高者（同价值取实例编号最小），其余联动猪
  可被批量售卖/做菜；`/开启批量保留` 后额外保留一只价值最高的普通猪猪与一只
  价值最高的美食（美食仅批量售卖），`/关闭批量保留` 恢复默认。保留目标限定在
  本批操作的品质与解锁范围内，由 `EconomyRepository._batch_keep_ids` /
  `GameplayRepository._batch_keep_pig_ids` 在事务内查询后以 `NOT IN` 排除。
- 批量做菜范围：不填品质时默认只处理一至三星低星原料猪（原为 ≤5 星），
  `/批量做菜 四星` 等指定品质行为不变；联动保留与普通保留同样适用。
- 新增命令 `/开启批量保留`、`/关闭批量保留`（组件 34 → 36），走
  `EconomyService.set_batch_keep_highest`，玩家偏好按 `(scope, player)` 持久化。
- 自动回归为 pytest `195 passed`（新增 4 个测试：开关命令端到端、联动保一只最高、
  保留最高价值猪/美食、批量做菜低星范围）；Ruff、Python `compileall` 全部通过。
## 38. v1.6.9 保千猪切换修复与多实例编号验收

- 验收日期：`2026-08-07`。插件升级为 `local.pig-catcher v1.6.9`，Schema `14`、
  Asset Manifest `4`、Ruleset `12` 保持不变；无数据迁移。
- 根因：v1.6.7 将保千猪复制到 QQ 官方双群后，`handle_toggle_baogian` 仍只查询
  NapCat 两个群的写死 `template_id`，官方群玩家的保千猪永远匹配不到，误报
  “你还没有保千猪”。
- 修复：改为按玩家实际持有实例判定——`GameplayRepository.list_baogian_instances`
  以显示名快照 `保千猪` + 模板备用图非空识别四群副本；`toggle_baogian_instances`
  实例级切换；`GameplayService.toggle_baogian(identity, short_code=None)` 覆盖
  无保千猪 / 多只缺编号（列出编号）/ 编号无效（列出持有编号）/ 单只免编号 /
  指定编号五种分支。
- 命令 pattern：`^/切换\s+猪保千(?:\s+([0-9A-Za-z]{8}))?\s*$`。
- 自动回归为 pytest `197 passed`（新增 2 个端到端测试：官方群多只编号切换与
  单只免编号）；Ruff、Python `compileall` 全部通过。

## 39. v1.7.0 全量审计、批量保留与四群同步验收

- 验收日期：`2026-08-09`。插件升级为 `local.pig-catcher v1.7.0`，Schema `14`、
  Asset Manifest `4`、Ruleset `12` 不变；无需迁移生产数据库或重建素材包。
- 根因确认：Schema 14 开关和生产玩家偏好写入正常，但 v1.6.8 只在整批普通资产中
  保留一个最高价值实例，导致其他模板仍被清空；联动猪也从 v1.5.1 的“全部保护”
  错误降级为整批只留一个。旧测试只验证开关列和单模板场景，未覆盖真实语义。
- 修复后：所有联动猪在批量售卖/做菜中默认全部排除；开启开关后，普通猪和美食在
  本批品质范围内按模板各保留一只最高价值实例，同价值按实例 ID 稳定选择。
- 工程优化：售卖与做菜共用 `batch_safety.py`；开关只要售卖或做菜任一功能启用即可
  设置；`pyproject.toml` 与锁文件版本从遗留 `1.5.0` 统一为 `1.7.0`。
- 四群同步：两组映射固定为 `1092931381 ↔ 13E3A339`、`237716658 ↔ CEAB3520`；
  四个作用域各 8 猪 + 8 菜，正式目录自动比较名称、描述、品质、体型、食谱与效果。
- 全量离线回归：pytest `197 passed`；Ruff、Python `compileall`、`uv lock --check`、
  `git diff --check` 全部通过。生产库仅做只读 `quick_check`、Schema/偏好/授权核验，
  没有修改玩家资产、余额、图鉴或偏好。

## 40. v1.8.0 菜品、道具与六星独占规则验收

- 验收日期：`2026-08-09`。插件升级为 `local.pig-catcher v1.8.0`，Schema `14`、
  Asset Manifest `4` 不变，Ruleset `12 → 13`；不需要数据库迁移，正式素材媒体字节不变，
  但必须按新定义重建 Manifest 元数据。
- 公共高星菜：8 道四星菜和 7 道五星菜的效果签名全部不同；五星路线通过更高倍率、
  更大百分点或更多使用次数明确强于四星。一盒油炸猪只增加当前额度窗口 2 次，
  猪寿司拼盘增加今天当前及后续所有窗口各 2 次。
- 商城：消耗品由 10 种扩展到 15 种，覆盖三套抓猪概率、体型、肥瘦、收益、厨艺偏向、
  额外份量、降级保底、升星与收益倍率；幸运/超级幸运猪哨和主厨/超级主厨香料使用
  领域函数公示真实概率，超级道具价格按总体强度重新定价，不再机械要求大于 2000。
- 叠加边界：抓猪与做菜仍各只有一个装备槽；普通概率菜按组取最早兼容效果，体型菜可
  另行叠加；一猪六吃与超级主厨香料是明确可叠加例外，六星菜概率统一封顶 50%。
- 六星菜独占：兼容六星效果不受队列先后影响，始终优先并从纯基础权重结算；等级、
  饲料、厨具、属性、装备道具及普通菜均不参与且不消耗。结果图和纯文字回退共同展示
  最终概率、概率来源和被保留资源。
- 视觉验收：商城长图直接由领域商品表生成，完整显示 15 种道具、2 种升级和五组概率带；
  抓猪概率区触发动态卡高，做菜结果只显示一份最终概率。Chromium DOM 检查无裁切、
  无越界、无破图，动态猪卡保留原帧数与时序。
- 四群一致性：公共规则天然共享到 `qq` 双群与 `qq-official` 双群；四份群专属
  8 猪 + 8 菜定义保持全字段一致，玩家资产、额度与效果队列仍按作用域隔离。
- 离线验收：pytest `206 passed`；Ruff、Python `compileall`、`uv lock --check`、
  `git diff --check` 与 Asset Manifest 构建/隔离导入校验全部通过。

## 41. v1.9.0 RAISE A SUILEN 联动与 Git LFS 全素材验收

- 验收日期：`2026-08-09`。插件升级为 `local.pig-catcher v1.9.0`；Schema `14`、
  Asset Manifest `4`、Ruleset `13` 均不变，本轮只增加公共联动模板与正式媒体。
- 新增 RAISE A SUILEN 完整 `5/5` 收集组：LAYER猪与摩托猪（MASKING）为四星；
  LOCK猪、PAREO猪与 chuchu猪（CHU²）为五星。正式源路径修正用户文件名中的
  `LCOK → LOCK`，角色 ID、固定槽位和官方资料 URL 均通过目录回归。
- 角色描述依据官方资料并结合原图中的乐器、服装、应援、蛋糕、DJ 台与肉干等元素撰写；
  所有文案为预审静态文本，运行时不调用 LLM，也未下载或混入官方角色图片。
- 正式目录为 215 项模板（152 猪 + 63 菜）、167 份唯一二进制、218 份隔离存储媒体、
  64 项群专属和 43 只联动猪；目录哈希为
  `9abe2e896b9bb42cd5bac1685b55115cd4767ddf2eac201f27134033d50b9b28`。
- 两个数字 QQ 群和两个 QQ 官方群各自可见 128 只猪、39 道菜和 8+8 个保密群槽位；
  四份完整图鉴均无裁切、越界或破图，五张新资产卡为 1128×820 且描述完整。
- `asset_library/current/` 的完整正式包按维护者要求纳入 Git LFS，历史归档与临时素材
  仍被忽略；构建报告改用仓库相对路径，并以 `ASSET_NOTICE.md` 说明代码与媒体权利边界。
- 更新公告按既有运营模板写入 `docs/10-update-announcement-2026-08-09.md`，逐项介绍
  五只联动猪、15 种商城道具、15 道公共四/五星菜、叠加规则与六星独占层。
- 离线验收：pytest `206 passed`；Ruff、Python `compileall`、`uv lock --check`、
  Manifest 构建/隔离导入、215 张资产卡、16 组动画代表帧与四作用域完整图鉴均通过。

## 42. v1.9.1 概率单调性与永久成长平衡验收

- 验收日期：`2026-08-09`。插件升级为 `local.pig-catcher v1.9.1`；Schema `14`、
  Asset Manifest `4` 不变，Ruleset `13 → 14`，无需数据库或素材迁移。
- 回归根因：旧链先叠加饲料/等级，再按基础权重比例换算道具目标；四星成长步长高于
  六星，最终归一化会让幸运猪哨、超级幸运猪哨和星辉探猪镜下的六星概率反向下降。
- 修复顺序：概率道具先建立公开基准；等级与饲料再通过只从一至三星供资的单调高星层
  叠加。基础、幸运、超级幸运和探猪镜四套基线均逐档保证 4/5/6 星不下降。
- 数值结果：饲料每成长档对 4/5/6 星相对权重分别 `+2%/+4%/+6%`；Lv.5 单独使用时
  实际为 `8.64%/4.71%/1.28%`，高星合计 `14.62%`。玩家 Lv.21 单独效果与之相同。
- 做菜成长：厨具 Lv.0-5 为 `+0/+4/+8/+12/+16/+20%`；玩家等级封顶为 `+10%`，
  满级组合相对倍率 `×1.32`。普通 1 至 5 星原料的每个可达高档结果都不低于成长前；
  六星猪基础 `90%/10%` 及六星菜独占层保持不变。
- 普通定向菜：目标概率继续按既有倍率校准，但新增份额只从更低星级转移；四星效果不再
  压低五星/六星，五星效果不再压低六星。结果卡和文字回退继续展示最终六档概率。
- 离线验收：pytest `221 passed`；Ruff、Python `compileall`、`uv lock`、`git diff --check`
  通过。`tools/uat_cooking_and_economy.py` 在生产数据只读来源的隔离副本完成抓猪、做菜、
  六星独占、商城、升级、账本、售卖与重启幂等流程，共生成 `34` 张 PNG；商城长图无裁切、
  越界或概率文案溢出。首次 UAT 发现并修复验收配置遗漏关闭做菜冷却，未触碰生产玩家数据。

## 43. v1.10.0 群内插件管理员权限验收

- 验收日期：`2026-08-09`。插件升级为 `local.pig-catcher v1.10.0`；Schema `14 → 15`，
  Asset Manifest `4`、Ruleset `14` 不变。迁移前已使用 SQLite 在线备份生产库，并在备份
  隔离副本完成 v14 到 v15 的逐级迁移、`quick_check` 与 `foreign_key_check`。
- 权限与范围：9 个新增显式命令组件全部只允许配置管理员执行，固定作用于消息所在
  `scope_id`。目标必须是当前群已登记玩家，可用结构化 `@` 或稳定用户 ID/OpenID，拒绝
  跨平台、跨群和昵称猜测；QQ 官方 Bot 不依赖数字 QQ 号。
- 经济：支持单人/全员发币与扣币，管理员扣币允许负余额；每位玩家各写一条不可变账本。
  全员操作使用单事务，故障注入验证中途失败时余额、账本、审计与回执全部回滚；普通经济
  路径继续通过条件更新禁止透支。
- 资产：猪猪与美食都可按名称或模板 ID 发放，可自动生成或手动指定全库唯一 8 位编号；
  发放不增加抓猪/做菜统计与奖励。删除要求“名称#编号”，会取消关联待处理交易、清展示位，
  并以 `admin-removed` 终态保留历史实例与图鉴；猪猪纪录引用会自动修复。
- 名单与额度：插件、赠送/收赠、交易名单独立维护；插件名单阻断普通命令及全部社交流程，
  配置管理员保留紧急解除权限。单人额度重置只改变目标当前窗口起点和冷却，不改写历史回执。
- 离线验收：pytest `227 passed`；管理员专项覆盖授权拒绝、群隔离、负余额、全员原子回滚、
  猪/菜发放删除、三类名单、帮助拦截、单人重置和命令正则。最终 Ruff、`compileall`、依赖锁、
  `git diff --check` 与生产库只读核验作为发布门禁。

## 44. v1.11.0 字母数字资产编号验收

- 验收日期：`2026-08-09`。插件升级为 `local.pig-catcher v1.11.0`；Schema `15 → 16`，
  Asset Manifest `4`、Ruleset `14` 不变。本轮仅改变新资产编号协议和查询规则，不改素材、
  概率、经济数值或既有实例内容。
- 编号协议：新资产允许 4 至 16 位 ASCII 字母数字；自动生成固定 8 位并从完整
  `0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZ` 选择。所有输入规范为大写，查询和唯一约束使用
  ASCII 大小写不敏感语义；交易订单号仍保持原协议，避免无关功能扩散。
- 管理员：`/猪管发猪|发菜` 可用空格尾参或 `名称#编号` 指定自定义编号，也可省略自动生成；
  猪猪与美食共享事务内冲突检查。删除允许使用任意大小写的同一编号。
- 全链兼容：详情、做菜、吃菜、售卖、赠送、交易、展示和保千切换均复用统一选择器；
  `/切换 猪保千` 的可选编号由固定 8 位扩为 4 至 16 位。
- 旧数据：生产迁移前 21,867 个猪编号和 5,686 个美食编号均符合新范围，跨类型及大小写
  冲突为 0。在线备份 `quick_check=ok`；隔离 v15→v16 迁移保持全部数量与抽样旧编号逐字一致，
  `foreign_key_check=0`，并验证大小写查询、重复拒绝和非法符号拒绝。
- 离线验收：pytest `229 passed`；Ruff、Python `compileall`、锁文件和 `git diff --check`
  作为最终发布门禁。生产热加载后 Schema 16、`quick_check`、外键和编号约束只读核验通过。
