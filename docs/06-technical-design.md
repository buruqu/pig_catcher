# 技术架构设计

> 当前2.0隔离开发目标为dev13 / Schema48 / Ruleset41 / Manifest4，SDK锁定2.7.1、
> 本地Python3.13.12。dev12全量结果见[验收报告](34-v2-final-acceptance.md)；dev13已完成
> 十层升级、主商城重平衡和三套独立器具商城的离线统一验收。下文1.x兼容基线和迁移历史保留原记录，
> 不表示本次启动、升级或修改了正式MaiBot。

## 1. 本机兼容基线

- MaiBot：`1.0.12`
- `maibot-plugin-sdk`：`>=2.7.0,<3.0.0`
- Python：项目兼容目标 `>=3.12`；当前 MaiBot `uv` 运行环境为 `3.14.4`
- 数据库：SQLite，异步访问
- 语言：运行日志、配置面板、命令和用户提示均以简体中文为首要实现

第五轮沿用已验证的 SDK 2.7.x 公共接口和宿主加载器；若后续版本变化，先更新兼容设计再写代码。

当前正式插件为 `1.29.1`，标识为 `schema_version=34`、`asset_manifest_version=4` 和
`ruleset_version=29`。三者独立递增，不能用插件版本代替数据、素材和数值规则版本。
Schema 18 为抓猪收据增加普通额度成本，Ruleset 16 增加六星菜专属抓猪、滚动七天到期、
多次六星效果与普通六星做菜平衡；Schema 19 修复存量“一猪六吃”效果类型与新参数不匹配；
Schema 20 与 Ruleset 17 同步 9 道公共四/五星菜的新效果、现有可用实例和未消费队列；
Schema 21 收敛热加载期间可能先落库的“猪利猪”早期参数；Schema 22 增加群级六星菜
时效和按玩家独立用量，并升级糖醋排骨、猪鼻蛋包饭存量快照；Schema 23 收敛猪鼻蛋包饭
复合效果、糖醋排骨隐藏参数和两道十连菜的小保底；Schema 24 为装备道具增加连续使用
剩余次数，并增加玩家级最后一份美食 30 秒确认状态；Schema 25 将耗尽状态统一表示为
队列记录不存在；Schema 26 保存六星菜永久进度，Schema 27 允许系统群效果资产赠送，
Schema 28 把猪鼻蛋包饭改为每玩家一次额外抓猪；Schema 29 将阿萨姆红茶奶雾锅的
自动赠送概率同步到新模板、现有可用/交易锁定菜品和未过期群效果；Schema 30 为抓猪额度
统计增加 `player_id + command_name + created_at + catch_quota_cost` 覆盖索引；Schema 31 为
猪猪和美食实例增加持久收藏标记及活动收藏查询索引，历史实例默认未收藏；Schema 32 增加
玩家术式资格、精确群运行中术式和苍赫组合进度；Schema 33 增加持久轮盘状态、允许同一道
来源菜生成多个奖励效果，并收敛六道重做菜的模板、实例和未消费效果；Schema 34 修复早期
已标记为 Schema 33 却仍残留来源唯一约束的数据库，并在快速启动路径加入关键结构校验；
Manifest 协议保持不变。

2.0 开发线从 Schema 35 / Ruleset 30 引入数据驱动成就系统。`achievement_definition_snapshots`
保存不可变定义版本；玩家档案、进度、事件、解锁、奖励库存、增量指标、群目标快照、回填状态、里程碑、
幂等操作和活动券分表保存。猪币相关指标由不可变账本触发器增量维护，日常命令不扫描历史账本；
星级、吃菜和巨物指标由成功业务事件继续累加。业务服务不直接调用“某个成就”的写库函数，而是在原业务事务成功
后消费不可变 `command_receipts`；同一玩家同一回执只有一个成就事件。成就事务失败不会回滚已
提交的抓猪/做菜，但原回执仍可安全重试。当前 1.x 正式库尚未执行 Schema 35。

## 2. SDK 映射

| 需求 | MaiBot 机制 |
| --- | --- |
| 用户显式命令 | `@Command` |
| 插件生命周期 | `MaiBotPlugin` 的加载、卸载和配置更新 |
| 中文配置面板 | `PluginConfigBase`、`Field` 和中文 UI 元数据 |
| 首页管理入口 | `@HomeCard` 声明式按钮，跳转插件配置 |
| 发文字与图片 | `self.ctx.send.text`、`self.ctx.send.image` |
| HTML 转 PNG | `self.ctx.render.html2png` |
| 当前群近期消息元数据 | `self.ctx.message.get_by_time_in_chat`，只计发送者、时间和命令过滤结果 |
| 持久化目录 | `self.ctx.paths.data_dir` |
| 临时渲染目录 | `self.ctx.paths.runtime_dir` |
| 日志 | `self.ctx.logger` |

插件不注册普通消息 `@EventHandler`、LLM `@Tool`、模型提供器或网关。监管只在赠送/接受交易
命令执行时按需查询当前群近期消息，不监听、保存或分析聊天正文。

## 3. 当前项目结构

第五轮运行结构如下：

```text
pig_catcher/
  _manifest.json
  plugin.py
  config.toml
  pyproject.toml
  README.md
  pig_catcher/
    commands/
      help.py
      context.py
      parsers.py
    domain/
      models.py
      enums.py
      gameplay.py
      economy.py
      social.py
      rules.py
      quota.py
      selectors.py
      errors.py
    services/
      assets.py
      framework.py
      gameplay.py
      economy.py
      social.py
      command_state.py
      maintenance.py
      quota.py
      restrictions.py
      receipts.py
    infrastructure/
      database.py
      migrations/
      repositories/
    rendering/
      animation.py
      adapters.py
      renderer.py
      models.py
      delivery.py
      templates/
    config/
      model.py
      access.py
  tests/
  catalogs/formal/             # 正式猪猪与美食元数据，进入 Git
  asset_library/current/       # 当前可导入素材包，仅本机保留
  tools/                       # 素材打包、导入与验收
  artifacts/                  # 仅本地视觉验收，Git 忽略
```

命令处理器只负责解析、权限、调用服务和发送；随机、价值、交易、纪录和资产状态都留在领域或服务层。
`/今日巨物` 直接以 `pig_instances + command_receipts` 的真实抓猪来源做只读窗口查询，
使用 SQLite `ROW_NUMBER()` 在当前作用域和北京时间当日内分别选出每位玩家的体型、重量最佳实例；
因此无需新增表或迁移，也不会把管理员发放资产、其他群资产或当前持有人误认为原抓取者。
Ruleset 6 将等级、猪饲料和厨具乘数定义为领域层纯函数；抓猪与做菜服务调用它们
结算，渲染适配器调用同一函数生成档案和商城数值，禁止在 HTML 中重新实现概率公式。
结算前数值等级写入随机快照，展示称号不进入快照或概率链路。
Ruleset 7 只把基础每日抓猪额度从 20 次调整为 22 次，不改变冷却、概率、额外次数、
经济或数据库结构。
Ruleset 8 保持六星猪 `90%` 五星、`10%` 六星的品质概率，但六星结果只允许使用
原料猪模板声明的同群对应菜，不再从当前群全部六星菜中随机选取。
Ruleset 9 将抓猪计数改为北京时间四窗口、每窗口 5 次，并把冷却限定在当前有效窗口；
群级手动重置通过审计事件推进有效起点。它同时大幅提高六星菜价值、奖励和三个定制
效果上限，并要求商城显示两种提质消耗品的实际概率。

紧凑列表不会把原始大图直接送入 Runner RPC。渲染器在线程池中解码素材，静态图取首帧、
动画取确定性的中间帧，缩放到最长边 256px 后编码为 WebP data URL；单资产静态卡也只传
最长边 768px、质量 90 的瞬时 WebP，原文件保持逐字节不变。衍生预览以路径、大小、修改时间、
尺寸和缓存协议组成稳定键，使用 8 MiB 进程内 LRU 与 64 MiB 数据目录磁盘缓存，并对同键
并发请求执行单飞。HTML 在调用 `render.html2png` 前执行 12 MiB 字节预检，从插件侧为
MaiBot 的 16 MiB RPC 帧保留协议封装余量。

## 4. 配置模型

当前中文面板分区：

| 分区 | 主要配置 |
| --- | --- |
| 插件设置 | 启用、配置版本、框架阶段 |
| 功能开关 | 帮助、抓取、档案、猪与美食背包/图鉴、做菜、食用、商城、升级、单项/批量售卖、账本、道具、展示和排行命令独立开关 |
| 访问控制 | 群黑白名单、用户黑白名单、Bot 管理员 |
| 数据存储 | 数据库文件、忙等待、只读并发、在线备份、全备份统一保留数和旧素材回退保留数 |
| 素材管理 | 清单文件、图片尺寸与大小、动画帧数与时长、暂存清理 |
| 抓猪规则 | 冷却、四个刷新小时、每窗口次数、六档权重和缺失六星策略 |
| 做菜规则 | 厨具十层上限、每层2%且满层20%、六星基础90/10与一次性美食效果参数边界 |
| 猪币经济 | 主商城单页、三个独立器具商城、账本分页、单次购买上限、饲料和厨具十层价格 |
| 赠送与交易 | 功能开关、交易价格上限和报价有效期 |
| 排行与巨物 | 综合榜权重、分页、绝对体型/重量门槛和目击展示数量 |
| 图片展示 | 字体、宽度、视口、超时、图片并发/排队、预览缓存、动画内存预算、PNG 上限和文字降级 |
| 额度重置 | 精确群号与一次性当前窗口重置开关 |
| 赠送与交易黑名单管理 | 精确群号、可选平台、成员 ID 列表、两类独立动作、原因和一次性执行开关 |
| 群公告发送 | 精确群号、可选平台、正文和一次性发送开关 |
| 运行维护 | 维护任务、首次延迟、轻量间隔、完整巡检间隔和完整性检查 |

关键规则配置需要交叉校验：

- 概率非负且归一化后总和大于零。
- 等级、价格、次数和期限在合理范围。
- 6 星基础做菜配置必须保持 90/10；只有清单中通过校验的 `next-six-star-cook` 一次性效果可在单次事务中临时调整。
- 黑名单优先于白名单，配置面板明确显示优先级。

## 5. 数据库

- 使用 `aiosqlite` 管理插件自有表，不把自定义模型塞进 MaiBot 核心数据库。
- 数据库默认位于 `ctx.paths.data_dir / "pig_catcher.sqlite3"`，文件名可配置但不能逃逸数据目录。
- 启用 `PRAGMA foreign_keys=ON`、WAL 和合理的 `busy_timeout`。
- 一个数据库连接管理器负责初始化、迁移、事务和关闭。
- 写事务使用单一进程锁和 `BEGIN IMMEDIATE` 串行化；只读事务使用最多 4 个短连接并发，
  强制 `PRAGMA query_only=ON`，关闭生命周期会等待全部活动读取结束。
- 仓储层不提交事务；事务由应用服务统一拥有。
- 迁移文件使用单调版本，测试空库升级和旧版本逐级升级。

Schema 3 为抓取频率、背包、图鉴和群纪录补充查询索引。Schema 4 为食物库存、
食物图鉴、官方售卖和猪币流水补充查询索引。Schema 5 新增稳定玩家统计、资产转移
审计、全群绝对纪录、巨物目击和交易并发索引，并为猪模板增加体型档案。Schema 6
新增 `player_food_effects`，保存一次性效果参数、发放/消费次数和有效期。Schema 7
为 `pig_templates` 新增 `paired_food_template_id`，持久化群专属六星猪的一对一食谱。
Schema 8 新增周与永久抓猪额度加成；Schema 9 新增 `player_restrictions`，Schema 10
把旧的统一社交限制无损拆成永久赠送/收赠黑名单、永久交易黑名单，并保留可到期的
每时段额度上限。Schema 11 为猪模板增加备用图片路径，Schema 12 为猪实例增加
`display_variant`，使同一实例可持久切换立绘与表情包；Schema 13 增加按窗口额度加成，
Schema 14 增加玩家级 `batch_keep_highest` 偏好。Schema 15 通过受检表重建移除余额非负
约束，新增运行时插件黑名单与 `admin-removed` 资产终态；迁移事务提交前执行
`PRAGMA foreign_key_check`，普通经济服务仍通过条件更新禁止透支。
Schema 16 再次只重建猪猪与美食实例表，把 `short_code` 约束改为 4 至 16 位 ASCII
字母数字并使用 `COLLATE NOCASE` 唯一索引。复制语句保留旧编号原始大小写和内容；服务层在
新建时统一大写，并在同一个写事务内跨猪猪、美食检查冲突。
Schema 17 新增 `anti_abuse_cases`、`anti_abuse_case_members`、`anti_abuse_notices`、
`anti_abuse_holds` 和 `anti_abuse_events`。自动限制不复用永久 `player_restrictions`，因此到期、
人工解除和永久黑名单不会互相覆盖。历史 `asset_transfer_events` 沿用 Schema 5 的范围时间索引，
不改表也不重写历史行。

### 自动监管事务

`RegulationService` 在赠送或接受交易的现有显式事务中读取滚动流转图，并把当前拟议流转作为
锚点分析。资产信号沿猪实例及美食 `source_pig_instance_id` 归并血缘；已接受交易同时保留价格
与官方价值，明显高价时建立买方到卖方的猪币收益方向。纯正常价格交易可以作为图谱上下文，
但不能在没有赠送或价格偏离时单独触发处罚。

事务开始前，`plugin.py` 通过公共 `message.get_by_time_in_chat` 能力最多读取当前精确聊天流
最近 5,000 条、30 天内的非命令消息。服务只聚合每名发送者的消息数和活跃日期，并使用
5 分钟内存缓存；正文、二进制内容和消息列表都不进入数据库。双方均达到 30 条/7 日且存在
双向赠送时，相关赠送事件才从本轮图排除。消息不超过 5 条/2 日、插件账号不满 7 天且玩法
动作不超过 10 次者进入疑似小号严格来源集合；查询失败时层级为 `unknown`，不做身份猜测。

判定顺序固定为：人工永久限制检查 → 解析配置管理员并排除其当前身份 → 监管单 24 小时到期处理 →
非管理员当前临时限制检查 → 排除管理员历史流转 → 图谱分析 →
案件与提醒/限制写入 → 允许时再扣款和转移。首次提醒生成 `PENDING` 通知，事务提交后由群消息
发送器领取；只有平台确认发送成功并写为 `SENT`，成员的 `warning_served_at` 才会设置。
后续重复消息先命中业务回执，不会再次增加监管事件。配置管理员完全不生成案件、评分、提醒、
限制或监管事件；管理员参与的历史流转也不进入后续图，但非管理员已有的活动限制仍优先阻断。

监管单的硬到期点固定为 `created_at + 24h`，后续证据只更新 `updated_at`，不得延长生命周期。
任何监管入口在读取限制、领取通知或列案前先惰性执行到期：案件改为 `dismissed`、分数和成员
累计归零、未发送通知失效、活动限制释放，并追加 `automatic-expiry` 历史事件。列表和案件详情
只查询未撤销且创建未满 24 小时的记录，因此不存在“界面消失但限制仍生效”的隐形处罚。

监管恢复使用 `tools/reset_regulation_state.py`。默认只读预览；`--execute` 先调用 SQLite 在线
备份 API，再在一个 `BEGIN IMMEDIATE` 事务中撤销案件、清零当前分数/成员累计、失效未投递通知
并释放限制。留空 `--scope-id` 时全局处理，每案追加 `global-reset` 事件并按涉及作用域追加
`automatic-regulation-bulk-reset` 通用审计；提供精确 `platform:group_id` 时只处理该群，改写为
`scope-reset` 事件与 `automatic-regulation-scope-reset` 管理审计。旧事件和已发送通知不删除。

群消息只消费预先生成的中性 `message_text`。内部 `score`、阈值和 `evidence_json` 不进入普通
回执、管理群回复或图片模型；管理员群命令只显示案件号、状态、相关账号和当前到期限制。

一次成功抓取在同一事务中
写入实例、奖励账本、余额、经验、图鉴、两类品种纪录、两类全群绝对纪录、巨物目击、稳定统计、额外次数、美食效果、道具消耗和命令回执；任一步骤
失败都会整体回滚。

一次成功做菜在同一事务中消耗原料猪、生成一份或两份美食、写做菜奖励、经验、
图鉴、美食效果、道具消耗与命令回执。购买、升级、食用和单项/批量官方售卖同样在单一事务中更新资产、库存、
经验、不可变账本、缓存余额与收据。渲染和发送始终发生在事务提交之后。

糖醋排骨食用、云海锅食用和糖醋排骨机会重置均直接把完整全群事件文字写入原业务回执。
事务提交后，命令适配器使用同一 `PENDING -> CLAIMED -> SENT/FAILED` 发送权渲染大事件图；
不创建第二条公告回执，也不调用主动消息或 `@全体成员` 接口。资格获取与真正重置是两笔
独立业务、两条独立消息回执，因此可以各自公示一次，同时不会把任一动作重复发送。

赠送在一个事务中转移所有权、更新双方稳定统计、写转移审计与收据。接受交易在一个事务中
重新校验角色、范围、状态、有效期、资产锁和余额，再写两条零和账本分录、转移资产、完成报价、
更新双方统计与收据。余额不足不改变待处理报价。

处罚工具先执行 SQLite 在线备份，再按精确 `scope_id + platform_user_id` 解析玩家。
三类限制、相关待处理报价取消/解锁和审计事件在一个事务中提交；任一玩家不属于目标群时整批拒绝。

群内 `AdministrationService` 只接受配置管理员入口：单人/全员猪币调整逐玩家写账本；资产
发放复用已授权模板与全库短编号校验；资产删除保留历史并修复失效纪录；三类运行时黑名单
和个人额度重置都按当前 `scope_id` 精确处理。每条改变状态的猪管命令把业务、审计与消息
回执放在同一事务中，重复消息不会重复发币、扣币、生成资产或重置额度。

面板的额度、黑名单和公告开关由同一生命周期处理器读取。处理器先一次性清除所有已领取
开关，再分别执行服务，保证配置热更新或重启不会重复产生副作用。公告按目标范围读取最近
`stream_id`，先写 `announcement-send-claimed`，调用 SDK 发送后再写成功或失败审计；失败
不自动重试。QQ 官方网关是否允许发送仍由主动消息权限或五分钟被动回复上下文决定。

### 5.1 并发

- 同一玩家的改变状态命令使用进程内异步锁串行化。
- 交易同时按稳定顺序锁定双方，避免死锁。
- 数据库唯一键和条件更新作为最终防线，不能只相信进程内锁。
- 重启后锁由数据库资产状态恢复，不依赖内存。
- 在线备份只使用独立备份锁，不占普通业务事务锁；备份先落到目标同目录临时文件，
  `quick_check=ok` 后才原子替换最终文件。卸载按备份锁、业务锁的固定顺序等待。

### 5.2 幂等

幂等键优先使用平台提供的消息 ID；若适配器缺少消息 ID，使用经过审计的稳定事件键，不用当前时间生成。

`command_receipts` 至少保存：

- 幂等键、命令、玩家、群范围
- 业务结果类型和业务对象 ID
- 文字摘要
- 渲染状态、图片校验值和发送状态
- 创建与最后更新时间

业务事务写入 `COMMITTED` 收据。重复事件查询到收据后不得重新执行领域服务。

发送状态采用 `PENDING -> CLAIMED -> SENT`，失败可记录为 `FAILED`。发送器先用条件更新领取一次发送权，再调用平台接口；进程若在平台已接收但状态尚未写成 `SENT` 时崩溃，重启后不自动重发 `CLAIMED` 收据，以牺牲极小概率的自动公示来保证不重复公示。用户仍可通过背包、详情或交易查询取得已提交结果。

## 6. 随机与规则版本

- 随机源通过接口注入，生产使用系统安全随机源，测试使用固定序列。
- 概率计算函数是纯函数，输入权重、升级、道具和资格，输出归一化权重与解释。
- 属性生成、价值、做菜品质和经验升级都使用纯函数并携带 `ruleset_version`。
- 每次结算把规则版本与关键输入写入实例或收据，方便复算。

## 7. 渲染管线

静态输出使用本地 HTML/CSS 模板和 `ctx.render.html2png()`：

1. 领域对象转换为无副作用的视图模型。
2. 对所有用户文本转义并限制长度。
3. 只引用插件资产和 `data_dir` 中当前群已授权素材。
4. 生成固定宽度、受限高度的 HTML。
5. SDK 返回 Base64 PNG 后，在线程池检查格式、解码、尺寸、文件大小和透明像素。
6. 浏览器渲染与 `ctx.send.image` 使用独立并发名额：默认同时最多 2 个渲染、4 个图片发送，
   慢 QQ 发送不会占用渲染名额。
7. 两个队列分别最多等待 12 秒；渲染或确定发送失败时记录分阶段耗时并发送文字摘要，图片
   发送超时且平台结果不确定时禁止补发，避免双重公示。

渲染器不访问互联网。模板样式和素材加载必须在离线环境可用。

动画输出先由 Chromium 生成不含素材的静态卡片底图，再逐帧把原始 GIF 或动画 WebP 合成到固定媒体槽中。合成器保留有效帧时长、循环和帧数；原素材缺少显式帧时长时使用配置回退值。合成后的待编码帧立即量化为调色板图，动画任务默认串行，并在建立整卡帧列表前按帧数、画布和最大 GIF 缓冲估算 256 MiB 工作内存预算；写出后关闭帧与缓冲，再按实际 GIF 大小复核 Base64 峰值。为防止编码器把连续相同画面合并，输出帧带有不可感知的单像素交替标记。原始素材始终按字节保存，不在导入或渲染前重编码。

第三轮提供抓取/详情单猪卡、档案、背包、图鉴、群纪录和道具回执模板。第四轮新增
美食详情、背包、图鉴、商城、账本和经济回执模板。第五轮新增 `NEW`、体型评价、全群巨物、
赠送/交易票据、交易列表、展示位和原创白粉排行榜。版本 1.2.0 在背包、图鉴和
排行榜中把动画素材的确定性中间帧转为内存 PNG 预览并显示动态标记；单资产视图仍
执行完整逐帧合成，原始文件不改写。
版本 1.19.0 新增 `GroupEventViewModel` 和 `group_event.html`，用于两道指定六星菜及糖醋排骨
正式重置；食物素材仍经有界 WebP 预览嵌入，重置事件无素材时使用本地 CSS 占位。
版本 1.19.1 把 `food_card.html` 的做菜概率从长副文案中拆到回执内的独立审计区；含概率卡
使用自然文档流承载回执和页脚，只保留首屏素材/详情的固定安全区，因此效果行增加时画布
随内容增长，不会覆盖概率、回执或页脚。普通美食详情仍使用原固定详情布局。

## 8. 素材导入

素材导入协议分为两个路径：

- 1 至 5 星公共素材：由版本化定义生成导入包，通过同一清单协议导入。
- 6 星群素材：落入本机暂存区，验证清单、图片、所属群和授权后再原子激活，不进入公共 Git 历史。

Asset Manifest v4 在 v3 的媒体、授权、`effect_id` 和 `effect_params` 上增加
`paired_food_template_id`，并校验六星猪和六星菜同群、一对一且完整覆盖。验证器按
文件内容而不是扩展名识别 PNG、JPEG、WebP 和 GIF，完整
解码所有帧并保存格式、动画、帧数、时长、循环和透明度元数据。文件路径通过解析后
的根目录校验，拒绝绝对路径、`..`、符号链接逃逸和无法识别的媒体。停用模板不删除历史行。

## 9. 安全与隐私

- 不记录 QQ 账号密码、Token、Cookie 或素材原始私密链接。
- 日志中的用户 ID 默认脱敏，调试级别也不输出完整交易对象。
- 6 星素材只在所属群渲染和查询。
- 管理操作写审计事件，包含操作者、范围、动作和结果。
- SQL 全部参数化。
- 资产描述只来自经过审核的静态数据，不把群聊文本拼成 HTML。

## 10. 可观测性与恢复

日志事件至少覆盖：

- 插件加载、数据库版本和素材统计
- 概率配置校验
- 抓取、做菜、售卖、赠送和交易的收据 ID
- 渲染与发送失败
- 账本不一致、资产状态冲突和迁移失败

维护任务支持：

- SQLite 一致性检查
- 数据库在线备份与所有插件备份族的统一保留数量清理
- 过期素材暂存目录清理
- 仅在生产库全部模板和全部保留备份均不再引用时清理旧不可变素材目录，并额外保留最新回退版
- 过期交易报价解锁
- 全库玩家缓存余额与不可变流水合计对账
- 当前全部模板素材文件存在性巡检

余额与不可变账本对账已在第四轮实现；第五轮补齐过期报价解锁。版本 1.24.0 首次维护默认
延迟 120 秒，轻量过期清理仍按小时执行，`quick_check` 和全账本对账默认每 24 小时一次；
在线备份使用独立锁，不再占用普通命令的全局事务锁。未知异常只记录日志，不自动篡改余额、
库存或业务历史。

运行 `backups/` 下所有直接普通 `*.sqlite3` 共享统一保留数量。旧 catalog 清理前读取当前
全部猪/菜模板（包括禁用模板的历史实例）和每份保留备份中的素材引用；任一保留备份无法
打开或缺少所需表时，本轮素材清理 fail-closed，不能以“无法证明仍引用”代替“已确认无引用”。
备份扫描完成后会再次读取活动模板；新发布 catalog 默认需度过 24 小时保护期才进入删除候选，
以隔离外部导入与后台维护的并发窗口。需要永久保留的发布保护点必须放到运行备份目录之外。

这些工具默认只读或需要显式确认，不在插件启动时静默修复未知问题。

## 11. 测试分层

| 层级 | 重点 |
| --- | --- |
| 单元测试 | 权重、属性、价值、做菜矩阵、效果兼容性、选择器、等级和权限 |
| 仓储测试 | 迁移、效果队列、约束、事务回滚、WAL 和重启 |
| 服务测试 | 抓取、做菜、食用、购买、升级、名称直选、收藏保护、按菜名批售、低星自动处理、批量售卖/做菜保护、账本对账、幂等、纪录、道具消耗、赠送、交易并发、展示和七类排行 |
| 渲染测试 | 关键模板截图、长名称、缺图、分页、中文字体、GIF 代表帧、DOM 不裁切和像素非空 |
| 插件加载测试 | Manifest、配置、命令注册、加载、卸载和重载 |
| UAT | 先用正式素材隔离副本跑真实 Chromium 命令流，再验证运行中 MaiBot 加载；真实 QQ 人机回归按用户决定由上线使用完成 |

批量资产安全规则集中在 `infrastructure/repositories/batch_safety.py`：普通资产使用
`ROW_NUMBER() OVER (PARTITION BY template_id ...)` 稳定选出每模板最高价值实例；联动猪也
按模板查询一个最高价值实例 ID 并加入保留集合，重复实例继续进入售卖/做菜候选。
四群内容同步由正式目录测试比较用户可见语义，玩家资产与账本仍按四个 `scope_id` 隔离。

Ruleset 13 把概率处理拆成普通叠加层与六星菜独占层。普通抓猪最多同时使用一个概率菜、
一个体型菜和一个装备道具；普通做菜最多同时使用一个概率菜和一个装备道具，明确允许
“一猪六吃”与超级主厨香料叠加并统一封顶。若存在与当前动作兼容的六星菜效果，服务层
直接从未应用等级、升级、属性、道具和其他菜品的纯基础权重开始结算；所有被跳过资源
保持原状，结果模型、幂等回执和渲染卡片共同记录独占来源及最终概率。

Ruleset 14 在普通叠加层内增加两个纯函数边界：`apply_monotonic_high_rarity_multipliers`
先计算相对权重候选，再把任何可能下降的 4/5/6 星档位固定在加成前，新增份额只由 1 至
3 星供资；`lift_target_rarity_from_lower` 按旧倍率校准目标概率，但只从严格更低星级转移。
抓猪概率道具先建立公开基准，再进入等级/饲料成长层。该顺序与最终权重一起写入随机快照，
避免 UI 公示、抽取逻辑和组合效果出现三套口径。六星菜独占层完全不变。

Ruleset 15 增加自动资产流向监管，不改变抓猪与做菜数值。Ruleset 16 将实际选中的六星
抓猪效果映射为专属次数：`command_receipts.catch_quota_cost` 对普通抓猪写 `1`、专属抓猪
写 `0`，仓储同时返回普通额度计数、全部成功计数和最后成功时间。普通额度门只使用前者，
冷却和违规处罚硬上限使用后两者，因此专属次数既不会占用普通额度，也不能绕过处罚。
多次数效果仍按 FIFO 和独占组选择，只有成功事务才递增 `consumed_uses`。

Ruleset 17 在普通效果层增加四个边界。第一，固定百分点的五星/六星抓猪增益通过
`_lift_five_and_six_by_points` 只从 1 至 3 星池供资，保证四星和未指定高星不下降。
第二，联动定向效果先把候选模板限制为当前作用域带联动集合的启用模板，再使用
`15/55/30` 的 3/4/5 星固定分布；概率资源被排除但不消费。第三，巨物五星复合效果在
品质结算后仅对五星候选模板追加巨物档案权重，不反向改变品质。第四，做菜结算支持最多
5 条猪饺队列与“一猪六吃”、超级主厨香料一起累加，统一封顶 50%；猪咪莓蛋糕则通过
五星概率下限函数为 1 至 5 星原料建立至少 85% 的五星结果，六星原料保持等待。

Schema 20 对正式模板、`ACTIVE/LOCKED` 美食实例和未消费效果队列做可重复的参数迁移。
迁移以正式菜名精确匹配，不做模糊匹配；已经消费完成或进入历史终态的数据保持原快照。
Schema 21 按“猪利猪”正式显示名修正模板、可用实例与未消费队列，用于兼容开发热加载
已经提前执行旧版 Schema 20 的生产库；该迁移重复执行仍收敛到同一参数。

Ruleset 18 在个人效果队列之外增加群效果层。`group_food_effects` 保存作用域、来源玩家、
来源美食、参数、起止时间和每玩家授予次数；`group_food_effect_usage` 以群效果与玩家组成
联合主键，延迟建立并原子递增。仓储查询同时联接来源玩家的 `platform_user_id` 和
`display_name`：前者只写入结构化随机快照与幂等回执供审计，后者才进入实际选中效果的
用户可见摘要，从而避免数字 QQ、OpenID 和内部玩家键泄漏到结果图。

神龙化猪七星云海锅优先进入纯基础独占分支；个人效果和装备道具保持未消费。普通群效果
在个人普通效果和道具之后应用，同一时刻只选择最强的一个群级六星倍率；联动固定分布
或个人六星独占接管时跳过且不署名。糖醋排骨的专属额度通过同一群效果使用表扣减，并将
抓猪回执 `catch_quota_cost` 写为 0。吃云海锅、吃猪鼻蛋包饭和执行糖醋排骨重置时，所有
逐玩家猪币分录、群效果、来源机会、审计与幂等回执共享事务，任一失败整批回滚。

Schema 22 创建上述两张表及作用域/玩家索引，并收敛糖醋排骨模板、可用/锁定实例和未消费
重置机会。猪鼻蛋包饭只更新模板和仍可使用/锁定实例，不把旧的个人待用做菜效果改成群级
发币，以免对已经食用并结算过的历史事件重新铸币。

Ruleset 19 不迁移历史资产。`CatchQuotaLayers` 统一基础、永久、滚动七天、当前时段、今日时段
和旧版共享池的正常额度计算；六星专属次数仍走 `catch_quota_cost=0`。批量安全由“全部联动
实例排除”改为“每个联动模板保留最高价值一只”。自动监管增加可解释的 `established / likely-alt /
unknown` 活跃层级和双向主账号赠送过滤，所有阈值均来自中文配置模型。

Ruleset 20 在既有六星独占与群倍率层上增加三个确定边界。猪鼻蛋包饭吃菜事务先创建群效果，
再为食用者创建 `next-six-star-cook {six_star_percent:60,uses:2}`；任一写入失败均回滚美食、
发币和两类效果。雾蓝键盘大福与猪保千猪排轮盘从持久队列的
`granted_uses - consumed_uses == 1` 判定最后一次，分别替换为 `20/30/50` 和
`10/10/10/10/10/50`，失败动作不扣次数。

糖醋排骨执行重置时把 `hidden_boost_chance_percent=10` 与两档 `10.04` 隐藏倍率写进群效果。
抓猪完成额度、处罚和冷却检查后，仅当该效果提供本次专属额度时多抽取一个随机值；命中后在
已经应用普通菜/道具的权重上按 `10.04 / 1.007` 比率替换群倍率，避免重复乘入基础 `1.007`。
随机值、命中标志、最终权重和提示摘要均进入抓猪快照，实际次数仍只在事务提交时消费。

Schema 23 通过正式菜名更新模板、`ACTIVE/LOCKED` 实例及尚未消费的个人效果，并更新仍有效
的糖醋排骨群效果。猪鼻蛋包饭只给未来食用创建双效果；已经完成吃菜结算的历史记录不补发
两次做菜机会，避免无来源授权的追溯赠益。

滚动七天额度在吃菜时用 `now + 7 days` 所在抓猪窗口的结束时刻作为到期点；Schema 18
迁移优先从来源美食 `disposed_at` 恢复实际食用时间，缺失时才回退额度行 `updated_at`。

Ruleset 22 不改变抓猪或做菜概率。它把配置管理员从当前与历史自动监管图完全排除，并把监管
案件、成员累计、通知和自动限制收敛到固定 24 小时生命周期；全局清零采用在线备份、原子状态
重置和追加式审计。Schema 25 足以表达这些状态变化，因此不新增迁移，也不删除任何历史证据。

Ruleset 23 将猪鼻蛋包饭群效果规范为 `dedicated_catches=1, dedicated_only=true`。每名玩家
第一次成功使用该效果时，五星、六星相对权重各 `×1.004`，`catch_quota_cost=0`，并在事务
提交时把该群效果的个人用量从 0 原子递增到 1；后续查询仍可读到尚未到期的群效果行，但领域
候选过滤会把该玩家已经耗尽的 `dedicated_only` 效果移除，因此不会继续加权。若更强普通群
效果仍有专属额度，本次先用其额度并保留蛋包饭；若更强效果只剩时效倍率，则权重继续选择
较高值，但 `dedicated_entry_id` 指向蛋包饭以提供额外额度。独占效果接管时两者均不消费。

Schema 28 精确更新四作用域猪鼻蛋包饭的模板、`ACTIVE/LOCKED` 实例，并把尚未到期的旧群
效果升级为每玩家 1 次。迁移不重发 1,004 猪币、不创建或修改个人两次 `60%` 做菜效果，也不
重写已过期群效果；旧的玩家使用表原样保留，整个迁移可重复收敛到同一参数。

Ruleset 27 把普通固定百分点、复制、六星独占抓猪和六星失败返还拆为独立领域效果。复制只
克隆结算后猪实例的模板与物理属性，并显式关闭第二份奖励、经验、统计、纪录与领域接管；
若本群正在运行伏魔御厨子、苍或赫，复制效果保留到下一次普通抓猪，避免群体归属规则歧义。
雾蓝、六档等概率和必出六星均在等级/饲料/道具/普通菜之前接管品质分布，被屏蔽队列保持
未消费。彩彩返还先用纯六星原料 `90/10` 决定成菜品质；只有五星结果才抽返还随机值并扣一
次保护，返还时五星成品仍正常生成，原料猪保持活动状态。

`player_roulette_state` 只保存玩家累计轮盘次数，不预抽结果。`/转轮盘` 在命令事务中先以
回执去重，再消费一次现有次数、抽取等概率 `1..6` 并原子写入猪币账本或对应效果。结果 2
追加两次后净剩余次数增加 1；结果 4 的额度到期点为当前额度窗口起点加一天；结果 5 和 6
进入独占抓猪队列。Schema 33 重建 `player_food_effects` 时保持全部既有行和外键，同时把
来源菜索引从唯一改为普通索引，使一份轮盘菜产生的多次不同奖励可以同时存在。Schema 34
对已发布但未实际完成该表重建的数据库执行同样的无损收敛，并要求当前版数据库必须同时
具备轮盘状态表、效果表和非唯一来源索引。

v1.28.0 为群体术式抓猪增加 `TechniqueCatchResolution` 回执快照。伏魔御厨子把实际生成的
两份美食编号、归属、素材信息和剩余领域次数写入抓猪回执；苍/赫写入最终归属者与剩余次数。
插件因此不再用“已被消耗的原料猪卡”代替出餐结果，重复消息也能从既有回执重建同一张结算
图。轮盘、术式发动、茈、专属菜资格与 KFC 全群扣款总和使用统一的大事件图片模型，但业务
结算仍全部先于渲染提交；图片故障不会回滚或重复执行业务事务。

v1.29.1 将五条猪御厨子结算升级为双模板事务。服务先验证苍蓝与赫焰模板均在当前精确群可用；
抓猪者就是领域发动者时两道菜都归发动者且不抽归属随机数，否则对按稳定玩家 ID 排序的已登记
玩家执行两次无放回随机抽取。随后消费原料并分别写入两道菜、两份图鉴和结构化随机快照。任意
模板、获得者、资产写入或回执步骤失败均回滚，领域次数只在完整结算成功后递减。公开图片区分
发动者全部获得与随机分配，稳定 ID 只留在回执审计。

### Schema 36：数据驱动周冲榜

周冲榜定义位于 `domain/weekly_competitions.py`，把期数、名称、业务来源、指标字段、聚合方式、
升降序、显示单位和奖励档作为不可变定义。首期使用 `official_value + sum + desc`；聚合器同时支持
`sum/max/min/count`，后续活动只需增加定义，无需复制命令、结算事务或 HTML 模板。

`weekly_competitions` 保存当前定义的规则快照和北京时间周窗口；
`weekly_competition_entries` 以活动与业务回执唯一，保存主资产的价值、品质、模板、显示名和发生
时间，不依赖当前背包状态；`weekly_competition_settlements` 以活动与精确群唯一；
`weekly_competition_awards` 同时约束玩家与最终名次唯一，并保存实际奖励和通知状态。

`WeeklyCompetitionService` 在功能初始化、榜单查询和结算前，从已提交 `pig-catcher.catch` 回执与
主猪实例快照进行幂等补录。正常抓猪交付路径在业务事务提交后增量处理，失败只影响榜单刷新，
不得回滚或重复抓猪。补录严格限制在单个活动周的有效业务来源；复制猪、管理员发放、纪念资产
与术式附带产物没有抓猪主回执，不能计分。

榜单查询按 `scope_id` 严格隔离。第一期同分依次比较累计价值、单只最高价值、有效抓取数、最后
计分时间和稳定玩家 ID。结算先完成补录，再在一个数据库事务内写结算、奖励库存、猪币账本和
待通知记录；所有表都有唯一约束，命令重试、插件重载和周一并发不会重复发奖。活动全局状态仅在
所有有成绩的作用域结算后冻结；首期结束不自动生成下一期。

### Schema 37：派遣与后续活动共用底座

独立 `domain/dispatch.py`、`services/dispatch.py`、`repositories/dispatch.py`，不把派遣公式
塞进抓猪服务或命令处理器。模板特长用稳定 ID 显式登记；规则版本、成员/属性/熟练度、旅费、
器具和计划时长在出发时冻结。四个专用斜杠命令接入既有访问控制、回执、图片和发送队列。

一次命令使用一个事务：去重 → 身份核验和到期推进 → 预览/确认/材料操作 → 保存结构化回执。
渲染只在提交后进行，不持有 SQLite 写锁；失败不会重跑扣费。确认有持久化两分钟 TTL 和队伍版本，
重放旧预览只取旧回执，不会覆盖玩家的新待办；同一群最后一项预览才可确认。

`FrameworkRepository.touch_identity` 在身份就绪后调用当前玩家的懒结算器。先获取最多三趟在途
记录，按块结束时刻与旅程序号合并排序，推进最多十八个整四小时块。共享路线探索/保底在这个
固定顺序中更新；随机种子与块/用途共同确定随机位置。不创建常驻线程或遍历所有玩家。
有效时间只增加未覆盖的区间，多个同时完成的队伍不重复解锁时长。

正常到期或召回统一经过 `finish`：材料及零头、器具变换、奇遇、纪念品、实例熟练、本人培养、
完成事实和占用释放都在当前事务。罗盘固定候选独立持久化，不阻塞其他奖励；新出发会先按既定偏好
结算遗留选择。召回未满四小时部分不结算，费用/器具不退；自然到期不能被并发确认倒改成召回。

`asset_occupancies` 以实例唯一，INSERT/UPDATE 校验所有权和群，猪实例触发器阻止忙碌时消费、
转移、交易锁定和删除。服务提供友好错误，自动选取/批量保留/批量写入同时排除占用，SQL 约束兜底。
只保存队伍时不加锁；安全旅行允许玩家精确点名收藏猪，但须预览确认，不放松其他收藏保护。

`material_ledger` 与 `activity_facts` 是不可变审计，前者保存定点增减和结余，后者按
玩家/来源/子事件唯一。基础、常规加成、自然奇遇、转换、制作来源分开，完成事实同时记录逻辑结束
与实际处理时间。实例熟练与本人贡献分表，以支持第四轮跨所有权、跨机制的成就判断。
当前不注册新成就或券；未消费事实不可当图片缓存删除。

历史、当前队伍、未读返程、待选奇遇均有针对性索引；输出分页。结构启动检查要求新表、
关键索引和保护触发器齐全。带旧玩家/资产/账本的 Schema 36 升级验证原表原行不变，
整体生产数据克隆升级和回滚彩排仍留到最终发布门禁。

### Schema 38：巡演、养成、保护与双向联演

版本化身份/曲目/组合目录与纯评分器在 `domain/tour*`，仓储保存档期、实例训练、本人贡献、
不可变阶段与收藏。`services/tour.py` 负责事务/回执，设置、联演和查询各自拆分，命令层只解析与交付。
Schema 38 增加 15 张表；原表不重建，旧数据迁移保持逐行一致，缺少关键表/索引/保护约束时拒绝启动。

每个玩家最多一个未完成巡演，联演最多一个待处理邀请，数据库唯一约束防止并发重入。
三站/一键用同一评分器和固定种子；联演接受在一个事务中完成双方，各自档期/器具/成长/奖励整体成败。
站间不持有活动占用；继续时再验证当前成员，已完成阶段不可改写。两分钟个人确认与五分钟联演邀请
分别持久化，晚处理按时钟过期，不建立逐用户后台计时器。

独立乐队保护接入旧资产筛选、底层消费和 SQL 触发器，转出要显式解除，转入训练猪恢复保护。
第四轮事实保留本人有效经验、自然材料支付、逐站与双人结算稳定引用；不注册未实现的成就或券。
图片和完整文字来自同一结构化视图，复用渲染限流与媒体重授权。入口/事实/复现见
[巡演交付说明](24-tour-implementation-and-acceptance.md)。

### Schema 39：版本化对战状态机与五次交付

`domain/battle_catalog.py` 管理招式/器具/成本，`domain/battle.py` 是无 I/O 的轮盘与状态机；
`services/battle.py` 管事务及回执，设置、独立战利品和视图各自拆分，七个 SDK 入口只做解析和交付。
新增 13 表、关键约束启动校验与共用占用/独立保护，不改旧库表结构，不修改 MaiBot 核心或 QQ 适配器。

群槽位、接受日角色额度、随机游标、逐招和回合快照、双向交付在事务中持久化。
每条指令最多执行 32 招，剩余可续；无限核心不转浮点、不调低整数字符串安全限制。
SHAKE256 分域及拒绝抽样确保双方操作先后、重试与分片不改变本场结果。
战利品分离于普通抓猪副作用链，仅复用永久权重、属性、冷却和合法模板筛选；不额外计经验、猪币或周榜。
图片/文本共用结构化视图；失败降级不回滚，历史重放重授权媒体。完整事实契约与验收见
[第三轮交付](25-battle-implementation-and-acceptance.md)。

### Schema 40：三系统成就消费者与实际用券

运行目录 `domain/data/activity_achievements_v1.json` 与设计资料分开；48 项条件由纯 reducer 读取
不可变事实推进，不把处理命令时间当作活动发生时间，不把当前玩家资产/余额倒推为自然经历。
`services/activity_achievements.py` 按 scope 读取未处理队列，每批 64 条、单次最多四批，
达到约 250ms 时在批次事务结束后让出；不在单条用户命令里重扫整库。待办有部分索引，空队列快速返回。

业务先提交，消费者才原子更新玩家投影、进度、解锁、奖励与队列处理标记。无新解锁时不生成冗余
成就事件，未变进度不重复写；原始事实仍保留。币奖去重键包含 player_id；奖励触发旧财富成就时
使用有界固定点循环，全部依赖稳定解锁键保证只发一次。历史数据使用个人汇总，不广播全群历史。

`achievement_coupons` 仓储供派遣/巡演/战斗共用，用途槽分离、业务来源去重、不可变消费账本；
预览绑定选择快照，成功业务才消费，图片失败不回滚或重发。奖励材料和券产出有独立来源分类，
不能反向刷“自然材料”成就。成就查询也可推动积压处理，未知版本拒绝处理且保留重试证据。

新命令仍走 SDK 公开能力、结构化视图及现有渲染/发送队列，无逐玩家计时器、LLM 或普通聊天监听。
状态和恢复/容量边界见 [第四轮交付](26-activity-achievements-and-v2-acceptance.md)。

### Schema 41 / Ruleset 36：第九期物理档案与独立展示标签

`assets/models.py`校验可选`display_tags`（最多5个、每个20字符、去重、拒绝控制符）；
`AssetRepository`在既有目录原子导入中保存`pig_templates.display_tags_json`。
它与食谱标签、旅行特长、collection身份彼此独立。正式205猪全部显式配置上下限，旧清单仍可缺省标签。

`domain/display.py`集中处理t/m展示、原单位明细及标签解码；渲染和文字降级使用同一函数，
不以字符串单位参与值比较。`DispatchRepository.member`直接读取实例百分位，BattleRepository复用该成员，
阻止模板重标定影响旧培养。运行中的行程和场次继续使用已冻结快照。

抓猪新回执保存标签快照，重放仍走当前权限；派遣/巡演/对战历史卡的媒体重授权同时清除不可见标签。
大范围变化不改旧实例、旧账本或货币计算；新产出仍由现有星级/百分位公式估价。
`test_physical_profile_stability.py`验证重导入、重启、派遣/对战能力和巨物候选范围；
`test_round9_catalog.py`验证完整覆盖、四scope一致、原图SHA及动画帧序。

### Schema 47 / Ruleset 41：十层升级与主商城重平衡（实施中）

领域层把猪饲料最大等级扩展为10，每层4/5/6星相对权重分别增加1%/2%/3%；玩家数值等级
仍使用原有独立步长，不能错误引用减半后的饲料步长。厨具扩展为10层、每层高档菜相对权重+2%。
两条Lv.10均与旧Lv.5效果完全一致，抓猪单调高星和普通做菜可达档不下降约束不变。

Schema47只迁移永久升级等级：每行旧等级原子变为 `min(10, old_level×2)`，升级类型、玩家、群、
账本和历史回执不变。迁移必须幂等并通过启动结构检查；不补写虚构购买账本、不追收新旧价格差。
十档价格与16项商品价格都来自配置/领域单一来源，商城图、文字帮助和购买事务不得另存手写副本。
三种概率道具先建立 `35/27/16/12/7/3`、`29/23/15/15/12/6`、`0/0/47/30/18/5`
基准，再进入等级/饲料单调层；Ruleset41用于区分旧回执与新结算。

### Schema 48：三个独立器具商城（实施中）

派遣、巡演、对战各自保留版本化器具目录和原材料制作服务；新增商城定义只引用既有稳定器具ID，
不复制效果、库存或使用状态。`/猪猪商城 派遣|巡演|对战`显式打开对应页面，主商城无参数页和
`全部`永不合并器具。`/购买 <器具名> [数量]`在一个事务内扣猪币、增加原系统库存、写不可变
猪币账本与器具购买审计；重复消息由现有回执键去重，事务失败不留下单边扣款。

购买与制作汇入同一库存，不记录不同强度或购买版本。派遣确认出发即消费、巡演站点成功才消费、
对战接受时预留并按触发/终局规则消费或退回，均继续由原玩法服务负责；商城层不得绕过携带上限、
占用、回滚或材料制作成就边界。本节是dev13实施目标，迁移、并发、图片和恢复验证尚待统一验收。
